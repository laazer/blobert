[pytest]
# Custom pytest markers for M902-17 test suite
markers =
    adversarial: Adversarial test case (schema violations, edge cases, failure paths)
    edge_case: Edge case test (boundary conditions, stress tests)
    integration: Integration test (end-to-end pipeline)
