# Bundled agent skills (blobert)

Skills in this tree are **source-of-truth copies** for the repo. Install into your agent client by symlink or copy:

| Skill | Path |
|-------|------|
| Asset pipeline MCP | `agent_skills/blobert-asset-pipeline-mcp/SKILL.md` |

Example (Cursor): symlink `blobert-asset-pipeline-mcp` into `.cursor/skills/` so the folder name matches the skill. Full MCP setup: `asset_generation/mcp/README.md`.
