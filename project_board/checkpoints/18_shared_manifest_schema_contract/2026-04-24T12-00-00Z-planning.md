### [18_shared_manifest_schema_contract] PLANNING — Ambiguity: shared schema module location and type system choice
**Would have asked:** Where exactly should the shared schema module live? Should it use Pydantic only, TypedDict only, or both (Pydantic for API, TypedDict for domain)? What is the exact boundary between "shared" and "service-local"?
**Assumption made:** Create `blobert_asset_gen/api_schemas/` as a new subpackage under `asset_generation/python/blobert_asset_gen/` (or equivalent canonical package root). Use Pydantic models for all API request/response payloads; use TypedDict in domain layer where framework-agnostic. The shared manifest schema will mirror the existing `Manifest` TypedDict but live explicitly in an `api_schemas` module for cross-boundary parity.
**Confidence:** High

### [18_shared_manifest_schema_contract] PLANNING — Ambiguity: backward compatibility scope
**Would have asked:** What exact response shapes must remain byte-identical? Which fields can be normalized without breaking existing frontend clients?
**Assumption made:** JSON structure keys and nesting must remain identical. Field ordering is not guaranteed but semantic equivalence is required. Any normalization that changes key names or nesting will be flagged as a scope change.

### [18_shared_manifest_schema_contract] PLANNING — Ambiguity: test parity contract
**Would have asked:** What does "schema parity" mean concretely? Is it type-level equivalence, runtime serialization equivalence, or both?
**Assumption made:** Parity tests will serialize shared Pydantic models and compare against service-layer manifest structures at runtime. Tests assert that API payload validation produces identical data to service-manipulated manifests after round-trip.
