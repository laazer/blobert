### [M901-04] TEST_BREAK — adversarial suite extension

**Would have asked:** Should adversarial tests cover `from .demo` relative imports under `utils/` for R7, and should the full 5! subprocess permutation suite stay unbounded for CI time?

**Assumption made:** Absolute `src.utils.demo` / `from src.utils import demo` AST coverage is sufficient for R7 alongside orphan file removal; full 120 permutations stays as a single test (fail-fast on first cycle). Relative `demo` imports are deferred — a future regression that reintroduces a local `demo` module would likely still appear as an absolute import or duplicate file audit.

**Confidence:** Medium

---

### Checkpoints encoded as tests

- `# CHECKPOINT` in `test_m901_04_utility_file_consolidation_adversarial.py`: negative `variant_index` stem format; NaN behavior for `clamp01` after move to `validation.py`.
