### [M901-04] TEST_DESIGN — strictest defensible behavioral suite
**Would have asked:** Should `validate_glb_path` treat a directory path ending in `.glb` as `ValueError`, and must idempotency compare resolved paths vs raw paths?
**Assumption made:** Tests require `ValueError` for missing, wrong suffix, and zero-byte files; idempotency is asserted on repeated calls with the same `Path` argument; string paths must return an equal `Path` to the first successful call for the same file.
**Confidence:** Medium

### [M901-04] TEST_DESIGN — legacy import scan scope
**Would have asked:** Should the AST legacy-import gate include `tests/` before all imports are mechanically updated (expected massive RED until implementation)?
**Assumption made:** Spec R5/R9 require zero legacy imports under `asset_generation/python` **and** `asset_generation/web`; the scan includes `tests/` and excludes only this ticket’s helper modules that would self-trigger false positives.
**Confidence:** High
