# Checkpoint Log: M25-PTP Planning Run
**Run ID:** run-2026-04-16T00-00-00Z-planning
**Ticket:** `project_board/25_milestone_25_enemy_editor_visual_expression/in_progress/02_procedural_texture_presets.md`
**Stage:** PLANNING → SPECIFICATION

---

### [M25-PTP] PLANNING — GlbViewer texture overlay: new component or inline

**Would have asked:** Should the Three.js texture overlay logic live as a new `TextureOverlay.tsx` component or inline within `GlbViewer.tsx`?

**Assumption made:** Inline in `GlbViewer.tsx`, specifically inside the `Model` component (or a helper called by it) that already owns the `scene` and `animations` objects. A new `TextureOverlay.tsx` import would need to receive/pass `scene` references, creating unnecessary coupling. The ticket lists both as options; inline keeps the change minimal and co-located with the code that owns the Three.js scene graph.

**Confidence:** High

---

### [M25-PTP] PLANNING — Spec ticket type for spec_completeness_check

**Would have asked:** What `--type` flag should be passed to `spec_completeness_check.py` for the texture presets spec?

**Assumption made:** `generic`. This ticket is purely a UI/frontend control feature. It has no HTTP mutation endpoints (controls are read-side metadata), no destructive operations, no randomness policies, and no load-open selectors. `generic` requires no required sections, so the check will always pass — which is correct.

**Confidence:** High

---

### [M25-PTP] PLANNING — Python test file path

**Would have asked:** Should the Python test be at `tests/utils/test_texture_controls.py` (as the ticket specifies) or `tests/enemies/test_texture_controls.py`?

**Assumption made:** `tests/utils/test_texture_controls.py` as specified in the ticket, following the existing pattern of `tests/utils/test_mouth_tail_controls.py` and `tests/utils/test_eye_shape_pupil_controls.py`.

**Confidence:** High

---

### [M25-PTP] PLANNING — texture_mode serialization in options_for_enemy / allowed_non_mesh

**Would have asked:** Should `texture_mode` and all `texture_*` param keys be added to `allowed_non_mesh` in `options_for_enemy()`, or handled by a different validation path?

**Assumption made:** All texture control keys (texture_mode, texture_grad_color_a, texture_grad_color_b, texture_grad_direction, texture_spot_color, texture_spot_bg_color, texture_spot_density, texture_stripe_color, texture_stripe_bg_color, texture_stripe_width) must be added to `allowed_non_mesh` in `options_for_enemy()`. This follows the exact same pattern as mouth/tail controls. They must also be added to `static_defs` in `coerce_validate_enemy_build_options()` via extending from `_texture_control_defs()`.

**Confidence:** High

---

### [M25-PTP] PLANNING — texture_grad_color_a / texture_spot_color type: str vs hex

**Would have asked:** The ticket says color params are `str/hex`. Should they be declared as `type: "str"` or a new `type: "hex"` in the control def dict?

**Assumption made:** `type: "str"` with default `""` (empty hex string). The existing codebase uses `type: "str"` for hex fields (e.g., `feat_body_hex`, `extra_zone_body_hex`). Introducing a new `"hex"` type would require frontend changes in `ControlRow.tsx` and `BuildControlRow.tsx` — out of scope for this ticket. `type: "str"` with empty default is consistent.

**Confidence:** High
