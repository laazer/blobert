# M902-02 Static Analysis Gate Tooling — PLANNING RUN

**Run Date:** 2026-05-14T15:30:00Z  
**Ticket:** project_board/902_milestone_902_agent_predictabilitiy_improvements/01_in_progress/02_static_analysis_gate_tooling.md  
**Stage:** PLANNING → SPECIFICATION  
**Responsible Agent:** Planner Agent  

## Checkpoint Summary

This run decomposes the static analysis gate tooling feature into a structured execution plan executable by the Spec Agent.

### Scope Analysis

The ticket mandates:
1. **Python tools**: ruff, mypy, bandit, vulture, import-linter, semgrep, wemake-python-styleguide
2. **TypeScript/React tools**: eslint, typescript-eslint, eslint-plugin-react-hooks, eslint-plugin-sonarjs, eslint-plugin-boundaries
3. **Godot tools**: gdformat, gdlint
4. **Cross-repo tools**: jscpd duplication analysis
5. **Integration**: One orchestrated command + machine-parseable handoff metadata
6. **Shadow mode**: Configuration that runs but does not enforce (enforcement deferred to M903)

### Context from Completed Framework Ticket (M902-01)

- Gate runner (`ci/scripts/gate_runner.py`) exists and is operational
- Structured gate result JSON schema is defined (`gate-result-success.json`, `gate-result-failure.json`)
- The framework supports shadow/blocking modes and structured output
- Gates are registered in `gate_registry.json` with mode, category, and invocation metadata

### Ambiguities and Assumptions

1. **wemake-python-styleguide compatibility with ruff**: Both lint Python; scope/override needs clarification. Assumption: wemake is advisory (non-blocking) or a separate gate; primary gate is ruff.
2. **semgrep ruleset ownership**: Local rules vs remote registry. Assumption: Start with conservative local rules in `.semgrep.yml`; remote registry can be added in M903.
3. **Godot CLI availability**: gdlint requires Godot CLI. Assumption: Both tools are optional/deferred if Godot is unavailable in CI.
4. **Web backend Python scope**: The ticket says "Python tools on `asset_generation/python` (+ web backend if in scope)". Assumption: Web backend has separate Python suite; include Python backend linting if it shares the same pyproject.toml / tool config, else defer to separate ticket.
5. **jscpd configuration**: Duplication threshold and exclusions. Assumption: Conservative baseline (10+ line duplication) with generated/binary exclusions per CLAUDE.md guardrails.
6. **Documentation scope**: Ticket says "prefer minimal doc; update Milestone 902 README if that is the chosen index." Assumption: Update Milestone 902 README with a new section under "## Current Status" listing the static analysis bundle command.

### Confidence and Risks

**Confidence:** HIGH. The validation gate framework is complete and tested. Tool ecosystem is well-known. No blocking dependencies.

**Risks:**
- Tools may not install on CI environment (macOS vs Linux constraints for Darwin .so).
- Some tools (e.g., gdlint) may require external installation (not via pip).
- Existing codebase may have high violation counts; shadow mode must not break CI.
- Cross-tool configuration conflicts (e.g., ruff vs wemake rule overlap).

### Decisions Frozen

1. Static analysis gate is a **new gate** registered in `gate_registry.json`.
2. Primary entry command is via the gate runner: `python ci/scripts/gate_runner.py static_analysis_check ...`
3. Configuration is distributed across tool-specific files (`pyproject.toml`, `eslint.config.js`, `.semgrep.yml`, `jscpd.json`).
4. Shadow mode is the default (no CI failures until M903 enforcement pass).
5. Web backend TypeScript is out of scope for this ticket (no TS backend codebase found).

---

## Task Execution Plan

See ticket update with structured decomposition below.
