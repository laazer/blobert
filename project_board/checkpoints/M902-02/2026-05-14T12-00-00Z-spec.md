# M902-02 Checkpoint Log

## Run: 2026-05-14 12:00:00Z — SPECIFICATION

### Summary
Spec Agent producing comprehensive functional and non-functional specification for static analysis gate tooling integration. Execution plan decomposed into 9 sequential tasks.

### Key Decisions & Assumptions

#### [M902-02] SPECIFICATION — Tool ecosystem scope and wemake integration

**Would have asked:** Should wemake-python-styleguide be mandatory enforcement or optional advisory?

**Assumption made:** wemake is installed but run in **advisory mode** (report violations but do not enforce in CI during M902). Primary gate enforcement is ruff (E9, F, I). wemake violations are logged for M903 baseline setting. This avoids scope creep on legacy codebase while establishing the integration path.

**Confidence:** High

---

#### [M902-02] SPECIFICATION — semgrep ruleset ownership

**Would have asked:** Use remote semgrep registry or local rules?

**Assumption made:** Local `.semgrep.yml` with conservative hardcoded rules targeting security/style only. Remote registry deferred to M903. Local rules are deterministic, version-controlled, and do not require external API calls or subscriptions.

**Confidence:** High

---

#### [M902-02] SPECIFICATION — Godot static analysis tool availability

**Would have asked:** Should gdformat and gdlint be mandatory or optional?

**Assumption made:** Both tools are optional for this milestone. If Godot CLI is unavailable, skip gracefully. Task 6 validates availability and checkpoints a fallback strategy (log skip vs defer to M903 task). Spec includes hook structure for future integration.

**Confidence:** High

---

#### [M902-02] SPECIFICATION — Web backend TypeScript scope

**Would have asked:** Should web backend FastAPI Python code be included in Python static analysis?

**Assumption made:** Yes. `asset_generation/web/backend` is FastAPI Python and shares tool config with `asset_generation/python`. ESLint applies only to `asset_generation/web/frontend` React/TypeScript code.

**Confidence:** High

---

#### [M902-02] SPECIFICATION — jscpd duplication thresholds and exclusions

**Would have asked:** What duplication line count triggers a violation?

**Assumption made:** jscpd threshold is 10+ lines (default). Excludes `*.glb`, `node_modules/`, `.venv/`, `reference_projects/`, and generated asset exports per CLAUDE.md guardrails. Violations are logged in shadow mode; enforcement toggle deferred to M903.

**Confidence:** High

---

#### [M902-02] SPECIFICATION — Script orchestration vs Taskfile only

**Would have asked:** Should static analysis be a pure Python script or also a Taskfile task?

**Assumption made:** Both. Core orchestrator is `ci/scripts/static_analysis_check.py` (deterministic, CI/local). Taskfile task `hooks:static-analysis` wraps the script for convenient developer access. Developers run `task hooks:static-analysis` locally; CI invokes the Python script directly.

**Confidence:** High

---

### Tool Availability Decisions (Task 2 & 6 Validation)

All Python tools are assumed installable via PyPI. If Task 3 encounters installation conflicts during `uv sync`, the checkpoint will record the conflict and the spec will mark that tool as deferred.

Current assumption: All tools install cleanly with Python 3.11. npm tools install cleanly with Node 18+.

---

### References
- Ticket: `project_board/902_milestone_902_agent_predictabilitiy_improvements/01_in_progress/02_static_analysis_gate_tooling.md`
- Gate framework: M902-01 COMPLETE (220 tests PASS)
- Execution plan: 9 tasks, each independently executable
