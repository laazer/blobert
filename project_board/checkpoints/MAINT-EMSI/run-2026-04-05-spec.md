### [MAINT-EMSI] SPECIFICATION — invalid scale handling
**Would have asked:** Should invalid `scale` (NaN, inf, zero, negative) clamp, default to 1.0, or raise?
**Assumption made:** Raise `ValueError` for any non-finite or non-positive `scale` at construction time (EMSI-2).
**Confidence:** High

### [MAINT-EMSI] SPECIFICATION — scaling mechanism vs. tests
**Would have asked:** Is root-empty uniform scale acceptable if tests only assert kwargs on mocked `create_sphere`/`create_cylinder`?
**Assumption made:** Spec allows equivalent transform approach but defines tuple-multiply contract as the primary observable for mocks; implementer should align tests with chosen strategy; tuple multiply is the reference strategy for EMSI-3.1/3.2.
**Confidence:** Medium

### [MAINT-EMSI] SPECIFICATION — public attribute name
**Would have asked:** Store scale as `model_scale` vs `scale` to avoid shadowing concepts?
**Assumption made:** Use instance attribute `scale` to match the constructor parameter and ticket wording (EMSI-1).
**Confidence:** High

### [MAINT-EMSI] SPECIFICATION — rotation_euler
**Would have asked:** Should arm/limb `rotation_euler` radians scale with `scale`?
**Assumption made:** No — angles stay identical at all valid scales; only positions and primitive scale tuples scale (EMSI-3).
**Confidence:** High
