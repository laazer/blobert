### [M901-06-animated-build-options-consolidation] TEST_DESIGN — primary behavioral contracts
**Would have asked:** Should public validation entrypoints be asserted as `normalize_controls` and `validate_build_options` at the package boundary, given the frozen module breakdown and acceptance criteria?
**Assumption made:** Yes. Primary tests encode these entrypoints as the consolidation contract, alongside `options_for_enemy` and control-definition access, with runtime behavior assertions only.
**Confidence:** Medium

### [M901-06-animated-build-options-consolidation] TEST_DESIGN — local execution environment gap
**Would have asked:** Which project Python environment should be used for local test execution when `pytest` is unavailable in the default interpreters?
**Assumption made:** Leave tests authored and lint-clean; defer execution to downstream agents/CI environment with the project test toolchain.
**Confidence:** High
