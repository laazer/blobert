# M11-10 — Carapace Player Attack (Specification)

**Run ID:** 2026-05-26T-spec-run  
**Agent:** Spec Agent  
**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/10_carapace_player_attack.md`  
**Stage transition:** SPECIFICATION → TEST_DESIGN

---

## Spec Output

Frozen spec at `project_board/specs/carapace_player_attack_spec.md`.

- **Spec ID:** CCA
- **Requirements:** CCA-1 through CCA-8 (11 + 4 + 11 + 6 + 6 + 7 + 7 + 6 = 58 acceptance criteria total)
- **Edge cases:** 22 entries
- **Discrepancy resolutions:** 6 (CCA-DR-1 through CCA-DR-6)

### Key decisions frozen:

1. **Radial query center:** Player position, no facing offset (CCA-DR-1).
2. **Wind-up:** `startup_frames = 12` (0.2s at 60fps) — reuses existing field (CCA-DR-2).
3. **Airborne deferral:** Executor-internal polling loop, 50ms interval, 3.0s timeout (CCA-DR-3).
4. **VFX signal:** New `slam_vfx_requested(position, radius, color, scale)` — distinct from melee signal (CCA-DR-4).
5. **Cooldown timing:** Starts at button press (existing behavior), unaffected by airborne wait (CCA-DR-5).
6. **Async lifecycle:** `_handle_slam_aoe()` owns `_is_active`; `execute_attack()` returns after dispatch (CCA-DR-6).

---

## Checkpoint Entries

### [M11-10] SPECIFICATION — Slam damage value

**Would have asked:** What damage value should the carapace slam deal? Ticket says "take damage" but does not specify a number.

**Assumption made:** `damage = 4.0`. This makes carapace the heaviest single-hit base mutation (claw=3.0, acid=1.0). Combined with the slowest cooldown (3.5s), this gives carapace the highest burst-per-hit but lowest DPS, reinforcing its "heavy" identity. Against 10 HP enemies, a slam + existing claw hit kills.

**Confidence:** Medium

### [M11-10] SPECIFICATION — Knockback magnitude value

**Would have asked:** What knockback magnitude should the slam have?

**Assumption made:** `knockback_magnitude = 5.0`. This is 2.5x claw's knockback (2.0). A ground slam should create significant displacement, clearing space around the player. The "away" direction means all enemies are pushed radially outward.

**Confidence:** Medium

### [M11-10] SPECIFICATION — Carapace color for VFX placeholder

**Would have asked:** What color should represent carapace in the VFX placeholder system?

**Assumption made:** `Color.SADDLE_BROWN` — an earth-tone brown that evokes shell/armor/carapace. Distinct from claw (`Color.ORANGE_RED`) and acid (`Color.CHARTREUSE`).

**Confidence:** Medium

### [M11-10] SPECIFICATION — execute_attack async lifecycle for SLAM_AOE

**Would have asked:** How should `execute_attack()` handle the fact that `_handle_slam_aoe()` needs to keep `_is_active = true` through its entire async duration (wind-up + airborne wait), while existing handlers don't need this?

**Assumption made:** Add a `return` after `_handle_slam_aoe()` in the match arm so `execute_attack()` does not reset `_is_active`. The handler manages its own `_is_active = false` at completion or timeout. This is minimally invasive — only the new arm is affected; existing MELEE_SWIPE and PROJECTILE_SPIT arms are unchanged.

**Confidence:** High
