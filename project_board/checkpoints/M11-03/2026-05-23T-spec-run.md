# M11-03 Spec Agent Run — 2026-05-23

**Run id:** `2026-05-23T-spec-run`  
**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/03_input_action_mapping.md`  
**Stage:** SPECIFICATION → TEST_DESIGN

---

## Outcome

- Spec: `project_board/specs/input_action_mapping_spec.md` (revision 1, type `generic`)
- Ticket stage: `TEST_DESIGN`, revision 3
- Handoff: `handoff-latest.yaml` (spec → test_designer)
- Spec exit gate: PASS

---

## Frozen Decisions

| Topic | Decision |
|-------|----------|
| Spec path | `project_board/specs/input_action_mapping_spec.md` |
| Normative prefix | `IAM-*` (10 requirements) |
| Current InputMap | 9 actions in `project.godot`; no `attack`/`menu` yet |
| Planned InputMap | `attack` (J), `menu` (Escape) — registration deferred |
| Aliases | `mutate` → `infect` (F); `swap_mutation` → `fuse` (G); no duplicate map entries in M11-03 |
| Combat consumption | `attack` > `infect` > `absorb` > `fuse`; menu alone suppresses all others same frame |
| Policy module | `PlayerInputActionPolicy` RefCounted — `normalize_action`, `is_action_permitted`, `resolve_consumed_actions` |
| Input phase | PFO Step 0 raw read; policy at Step 0c with previous-frame `get_state()` |
| FLOAT / MUTATE | Full matrix rows; FLOAT target for future float input; MUTATE denies same set as ABSORB |
| HURT / DEAD | Deny all gameplay; HURT permits menu only; DEAD denies menu |
| debug_kill | `debug_actions_enabled` hook; DEAD always denies |
| M11-03 boundary | No `PlayerController3D` / `project.godot` edits |

---

### [M11-03] SPECIFICATION — attack default key

**Would have asked:** J vs Z for `attack`?

**Assumption made:** Primary default **J** per M11-07 ticket; Z noted as settings-only alternate, not dual map default.

**Confidence:** High

---

### [M11-03] SPECIFICATION — menu in DEAD

**Would have asked:** Can pause menu open on death screen?

**Assumption made:** `menu` denied in DEAD (no actions in terminal state).

**Confidence:** Medium

---

### [M11-03] SPECIFICATION — HURT movement lock

**Would have asked:** Should HURT allow movement like today?

**Assumption made:** Target matrix denies all movement in HURT (M11 combat clarity); documents divergence from current ungated controller.

**Confidence:** Medium

---

## Spec Exit Gate

```
spec-completeness-check: input_action_mapping_spec.md  type=generic
PASS: all required sections present.
```

## Orchestrator Follow-ups

```bash
python ci/scripts/run_workflow_transition_gates.py --ticket-id M11-03 --transition spec_to_test_design
```
