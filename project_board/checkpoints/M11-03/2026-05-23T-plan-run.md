# M11-03 Planning Run

**Run id:** `2026-05-23T-plan-run`  
**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/03_input_action_mapping.md`  
**Stage:** PLANNING → SPECIFICATION  
**Planner:** Planner Agent

---

## Outcome

Planning complete. Ten-task execution plan embedded in ticket (revision 2). M11-01 prerequisite satisfied (`done/01_player_state_machine.md`). Greenfield spec at `project_board/specs/input_action_mapping_spec.md`. Handoff to Spec Agent. Orchestrator next: `planner_to_spec` transition gates.

---

## Repo Discovery (Planning Evidence)

| Asset | Path | Relevance |
|-------|------|-----------|
| InputMap (current) | `project.godot` `[input]` | `move_left`, `move_right`, `jump`, `detach`, `detach_2`, `absorb`, `infect`, `fuse`, `debug_kill` — **no** `attack`, `mutate`, `swap_mutation`, `menu` |
| Player input read | `scripts/player/player_controller_3d.gd` `_read_player_input()` | Axis + jump + detach slots + `debug_kill`; absorb/infect/fuse read elsewhere in controller |
| Gameplay FSM | `scripts/player/player_state_machine.gd` + `project_board/specs/player_state_machine_spec.md` | Ten states: IDLE, WALK, JUMP, FALL, FLOAT, WALL_CLING, ABSORB, MUTATE, HURT, DEAD |
| Frame order | M11-02 COMPLETE (`player_physics_frame_order_spec.md`) | Input read precedes FSM tick in frozen pipeline — spec must name **phase** for action polling vs consumption |
| Downstream attack binding | `to_update/07_attack_input_and_cooldown_framework.md` | Expects `attack` in InputMap (default J/Z) — must align with IAM defaults |
| Input policy tests | `tests/**/*input*action*` | **None** |

**Default key snapshot (physical_keycode → key):** A/Left `move_left`; D/Right `move_right`; Space `jump`; E `detach`; Q `detach_2`; R `absorb`; F `infect`; G `fuse`; K `debug_kill` (debug builds only).

---

### [M11-03] PLANNING — Spec-only vs InputMap registration

**Would have asked:** Does M11-03 add planned actions to `project.godot` or only document them?

**Assumption made:** M11-03 delivers **normative spec only** (no `PlayerController3D` edits). Registering new InputMap actions in `project.godot` is **deferred** to the first implementation ticket that consumes the spec (e.g. M11-07 attack framework), with spec listing **planned** actions and proposed defaults. Spec must table **current vs planned** actions with evidence paths.

**Confidence:** High (matches ticket “spec-only; no PlayerController implementation”).

---

### [M11-03] PLANNING — Legacy actions in matrix

**Would have asked:** Should `detach`, `detach_2`, `infect`, `fuse` appear in the state-action matrix?

**Assumption made:** Yes. Spec enumerates **all** gameplay actions (M1 + M11 planned), including chunk/mutation actions, with per-state permit/deny and consumption priority. Ticket example matrix is illustrative, not exhaustive.

**Confidence:** High

---

### [M11-03] PLANNING — FLOAT / MUTATE rows

**Would have asked:** Are FLOAT and MUTATE rows identical to ticket example for movement-only states?

**Assumption made:** Spec Agent derives rows from **current** controller behavior where observable, then documents **target** M11 gating for attack/absorb/mutate where behavior will change in downstream tickets. FLOAT: no hold-to-float today — matrix documents **future** permit set when float input lands. MUTATE: movement locked when mutation active per derivation priority.

**Confidence:** Medium

---

### [M11-03] PLANNING — Test strategy split (contract vs runtime)

**Would have asked:** Should M11-03 author RED tests without any implementation file?

**Assumption made:** After spec: Test Designer authors **runtime** tests against a **pure RefCounted** `PlayerInputActionPolicy` (name frozen in spec) that encodes matrix + consumption — same pattern as `PlayerStateMachine`. No prose/markdown asserts. **Out of scope for M11-03 implementation stage:** wiring policy into controller; tests stay RED until follow-up implementation ticket OR explicit extension of M11-03 if orchestrator keeps IMPLEMENTATION for policy-only module. Ticket COMPLETE when spec AC met + test suites exist (RED acceptable) + spec gate PASS; controller wiring remains downstream.

**Confidence:** Medium

---

### [M11-03] PLANNING — Default keys for new actions

**Would have asked:** What defaults for `attack`, `mutate`, `swap_mutation`, `menu`?

**Assumption made:** Spec proposes: `attack` → J (align M11-07); `mutate` → unused key TBD with no M1 conflict; `swap_mutation` → Tab or number row per M3 slot UX; `menu` → Escape — Spec Agent validates against `project.godot` and documents override/settings seam (future).

**Confidence:** Low — Spec Agent must freeze in spec with rationale.

---

### [M11-03] PLANNING — Spec path and exit type

**Would have asked:** `agent_context/agents/2_spec/` vs `project_board/specs/`?

**Assumption made:** `project_board/specs/input_action_mapping_spec.md` per user handoff and ticket AC. Spec exit: `--type generic`. Normative ID prefix: `IAM-*`.

**Confidence:** High
