### [M25-ESPS] TEST_BREAK — None coercion for pupil_enabled
**Would have asked:** Should `None` passed as `pupil_enabled` crash or coerce to `False`?
**Assumption made:** `None` is falsy; conservative assumption is it coerces to `False` (consistent with `_coerce_boolish` treating falsy inputs as False). Test marked `# CHECKPOINT`.
**Confidence:** High

### [M25-ESPS] TEST_BREAK — Whitespace-only string for eye_shape / pupil_shape
**Would have asked:** Is `"  "` (whitespace only) treated as invalid and fallen back to default?
**Assumption made:** Whitespace-only is not in the valid options list → falls back to default. Test marked `# CHECKPOINT`.
**Confidence:** High

### [M25-ESPS] TEST_BREAK — Integer 0 / 1 for pupil_enabled in frontend store
**Would have asked:** Should the UI disable `pupil_shape` when `pupil_enabled` is integer `0` (not Python `bool` `False`)?
**Assumption made:** The frontend `buildControlDisabled` check uses a truthy/falsy test (not `=== false`), so `0` disables and `1` enables. This is the conservative assumption matching the spec's intent. Tests marked `# CHECKPOINT`.
**Confidence:** Medium

### [M25-ESPS] TEST_BREAK — list value for eye_shape
**Would have asked:** Should a list value for `eye_shape` (e.g. `["circle"]`) crash or fall back?
**Assumption made:** It is not a valid string option → treated as invalid → default `"circle"`. Test marked `# CHECKPOINT`.
**Confidence:** Medium
