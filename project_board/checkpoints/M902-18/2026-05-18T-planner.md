# M902-18 Tool Categorization Layer — Planner Stage

**Ticket Path:** `project_board/902_milestone_902_agent_predictabilitiy_improvements/01_in_progress/18_tool_categorization_layer.md`

**Run Date:** 2026-05-18  
**Stage:** PLANNING → SPECIFICATION  
**Agent:** Planner Agent

---

## Planner Analysis & Decomposition

### Summary

M902-18 implements a **tool categorization system** to reduce context overhead in agent execution by ~15–25% per stage. The feature applies Claude Agent SDK tool filtering to five agent-workflow categories (parse, modify, test, plan, think), allowing agents to declare a category and receive only relevant tool schemas.

### Key Findings

**Scope Completeness:** The ticket defines clear acceptance criteria (8 acceptance criteria, all testable).

**Dependency Status:**
- M902-01 (Validation Gate Framework): COMPLETE ✓
- Agent SDK modifications: **ASSUMPTION REQUIRED** (see below)

**Design Decisions Frozen in Ticket:**
1. Five categories: `parse`, `modify`, `test`, `plan`, `think` (with example tool assignments in ticket table)
2. Config storage: `ci/scripts/tool_categories.json`
3. Function interface: `get_tools_for_category(category: str) -> list[Tool]`
4. Agent opt-in or dynamic selection per workflow stage
5. Measurement required: token savings in checkpoint logs

**Clarifying Questions & Ambiguities:**

1. **CRITICAL: Agent SDK tool filtering interface**
   - **Question:** The ticket states "Use agent SDK's tool filtering mechanism to exclude non-matching tools" but does not specify which SDK version or API surface is available.
   - **Known:** Claude Agent SDK exists and agents are executing with current tool set (Read, Write, Glob, Grep).
   - **Unknown:** Does the SDK expose a public interface for filtering tools before agent invocation? Is this a hypothetical feature, or can it be integrated in this ticket?
   - **Assumption made:** The SDK supports tool filtering (either natively or via custom middleware). Spec Agent will clarify with SDK team if needed.
   - **Confidence:** MEDIUM

2. **Tool categorization boundaries (example vs. normative)**
   - **Question:** The ticket provides an example categories table with tool assignments (parse = Read/Bash-grep/WebFetch, etc.) but these are listed as "Example Categories" with "Use Case" column. Are these recommended heuristics or mandatory assignments?
   - **Known:** The ticket also states agents "must opt-in or dynamically select based on workflow stage" and provides acceptance criteria AC5 for agent declaration in input prompt.
   - **Assumption made:** The example table is normative for this milestone; Spec Agent will freeze exact tool-to-category mappings and rationale.
   - **Confidence:** MEDIUM-HIGH

3. **Measurement and token reporting**
   - **Question:** AC6 (token savings measurement) references "measure baseline (all tools) vs. category-filtered size" but does not specify what "size" means: JSON schema byte count, token count via API, or other metric?
   - **Known:** Checkpoint logs exist and are used for decision recording; token_budget_analyzer.py exists in ci/scripts.
   - **Assumption made:** "Size" refers to token count of tool schema JSON; Spec Agent will define measurement method and baseline protocol.
   - **Confidence:** MEDIUM

4. **Gating: Integration testing scope (AC7)**
   - **Question:** AC7 requires "Integration tested with 3+ agents (spec, implementation, test-designer)" — does this mean:
     a) All 3 agents must run in a single ticket and declare categories, OR
     b) The feature must be tested in 3 separate tickets (spec ticket uses parse category, implementation uses modify, etc.)?
   - **Known:** Current M902 pipeline has multiple agent stages; M902-18 is scheduled late in backlog.
   - **Assumption made:** Integration testing means the feature is used by 3+ different agent runs across the milestone, not all in this ticket. Test Designer will define test scope.
   - **Confidence:** MEDIUM-HIGH

5. **Spec reference missing**
   - **Question:** Ticket references `project_board/specs/902_18_tool_categorization_spec.md` but file does not exist (confirmed grep + read).
   - **Known:** Other M902 tickets have spec files; workflow enforcement requires specs before TEST_DESIGN.
   - **Assumption made:** Spec file will be created by Spec Agent; ticket reference is placeholder.
   - **Confidence:** HIGH

### Risk Areas & Mitigation

| Risk | Likelihood | Severity | Mitigation |
|------|------------|----------|-----------|
| SDK tool filtering API unavailable or different than assumed | Medium | High | Spec Agent must coordinate with SDK team in SPECIFICATION stage; document actual API surface before TEST_DESIGN. If API differs, adjust implementation plan (custom middleware vs. native filtering). |
| Tool-to-category mappings create false negatives (agent blocks needed tool) | Medium | Medium | Test Designer must create adversarial tests for boundary cases (parse mode using Read but needing Bash for non-grep task). Checkpoint decision log will track design rationale. |
| Token measurement proves impractical (no stable baseline or granular reporting) | Low | Medium | Fallback: use schema JSON byte size as proxy metric. Document in spec that "token savings" measurement is approximate pending API instrumentation. |
| Opt-in mechanism not adopted by agents | Medium | Medium | Mandate usage in agent input schema and workflow enforcement gates. Include in M902-08 runbook updates. Provide clear examples in checkpoint logs. |
| Integration testing deferred too late | Low | Medium | Assign early agents (Spec, Test Designer) to declare categories during their runs; log measured overhead in checkpoints. Verify 3-agent coverage before IMPLEMENTATION. |

### Gating Dependencies

**Blocking dependencies on M902-18 SPECIFICATION:**
- M902-01 (Validation Gate Framework): COMPLETE ✓ — non-blocking, provides gate infrastructure

**Gating dependencies FROM M902-18:**
- M902-19 through M902-23 may depend on tool categorization for context optimization. None explicitly listed in ticket.
- M902-17 (Final Validation) AC6 requires "15–25% context reduction verified" — M902-18 is this reduction feature.

**Conclusion:** No hard blockers. M902-18 can proceed to SPECIFICATION immediately. Spec Agent must clarify SDK API surface and define tool-to-category mappings.

### Design Decisions Frozen

1. **Five categories:** `parse`, `modify`, `test`, `plan`, `think`
2. **Config file location:** `ci/scripts/tool_categories.json` (or similar)
3. **Function interface:** `get_tools_for_category(category: str) -> list[Tool]` (or SDK-provided equivalent)
4. **Agent declaration:** Agents declare category in input prompt (e.g., "I'm in parse mode, give me read-only tools")
5. **Measurement metric:** Baseline (all tools) vs. category-filtered token/schema size
6. **Scope:** Tool schema reduction only; no logic changes to agents or workflow

### Assumptions Summary

| # | Assumption | Confidence | Resolution Owner |
|---|-----------|-----------|-------------------|
| 1 | SDK supports tool filtering (native or custom middleware) | MEDIUM | Spec Agent (must confirm in SPECIFICATION) |
| 2 | Example tool table is normative; Spec Agent will freeze exact mappings | MEDIUM-HIGH | Spec Agent |
| 3 | Token measurement via schema JSON byte count or similar proxy | MEDIUM | Spec Agent |
| 4 | Integration testing across 3+ tickets in milestone, not all in M902-18 | MEDIUM-HIGH | Test Designer Agent (will detail in test design) |
| 5 | Spec file placeholder will be created by Spec Agent | HIGH | Spec Agent |

---

## Execution Plan Output

Detailed execution plan provided separately in structured table format. Ready for Spec Agent handoff.

### Next Actions

1. **Spec Agent** reads this checkpoint and ticket
2. **Spec Agent** validates SDK API surface; escalates if filtering not available
3. **Spec Agent** freezes tool-to-category mappings with rationale
4. **Spec Agent** defines token measurement protocol
5. **Spec Agent** creates spec at `project_board/specs/902_18_tool_categorization_spec.md`
6. Proceed to TEST_DESIGN after spec completeness gate passes

---

**Execution Plan Status:** READY FOR SPECIFICATION
