# M902-28 Spec Run

**Run id:** `2026-05-20T-spec-run`  
**Ticket:** `project_board/902_milestone_902_agent_predictabilitiy_improvements/01_in_progress/28_parallel_hook_execution.md`  
**Stage:** SPECIFICATION → TEST_DESIGN  
**Agent:** Spec Agent

---

## Outcome

Specification authored at `project_board/specs/902_28_parallel_hook_execution_spec.md` (10 requirements, safety matrix, baseline protocol, Lefthook parallel contract, opt-out table). `spec_completeness_check.py --type generic` PASS. Handoff to Test Designer Agent.

---

### [M902-28] SPECIFICATION — LEFTHOOK_PARALLEL env var

**Would have asked:** Should blobert add `BLOBERT_HOOKS_PARALLEL=0` for developers?

**Assumption made:** No — Lefthook 1.x documents parallel only via `lefthook.yml` `parallel:` key; use `LEFTHOOK=0`, `LEFTHOOK_EXCLUDE`, or temporary `parallel: false`. Spec documents negative finding explicitly (Requirement 05).

**Confidence:** High

---

### [M902-28] SPECIFICATION — godot-tests ∥ py-tests safety

**Would have asked:** Is concurrent Godot import safe while pytest runs?

**Assumption made:** **SAFE** with disjoint writes (`.godot/` vs `asset_generation/python/coverage.xml`); Integration must validate with 3+ consecutive pre-push runs; rollback to `parallel: false` if flakes (Requirement 09).

**Confidence:** Medium

---

### [M902-28] SPECIFICATION — pre-commit parallelism proof

**Would have asked:** What evidence counts as “true concurrency”?

**Assumption made:** `LEFTHOOK_VERBOSE=1` log overlap or ≥25% wall-clock reduction vs sum of sequential single-hook runs on same staged set (Requirement 06); N/A when only one glob family triggers.

**Confidence:** High
