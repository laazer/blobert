# M25-04 Spec Agent Run — 2026-04-19T00-00-00Z

## Context
Stage: SPECIFICATION
Agent: Spec Agent

---

### [M25-04] SPECIFICATION — Euler order assumption
**Would have asked:** The ticket specifies `Euler('XYZ')` without confirming this matches the Blender convention used by the existing animated enemy rigs. No Blender rig file was available to verify.
**Assumption made:** Use `Euler((rx, ry, rz), 'XYZ')` exactly as documented in the ticket's execution plan (Task 4 code snippet). If an existing rig used a different order, that would need a separate corrective ticket. The spec mandates 'XYZ' explicitly so implementers and testers have a deterministic target.
**Confidence:** High (ticket explicitly prescribes the string; divergence would be a separate bug)

---

### [M25-04] SPECIFICATION — `_rig_rotation_control_defs()` location
**Would have asked:** The ticket assigns the new function to `animated_build_options.py` but the existing appendage/tail defs live in the extracted `animated_build_options_appendage_defs.py`. Should `_rig_rotation_control_defs()` be co-located there or in the main module?
**Assumption made:** Per ticket Task 1 ("File: `asset_generation/python/src/utils/animated_build_options.py`"), the function lives in the main module, not in the appendage defs file. The appendage defs extraction was for size reduction; rig rotation controls are a distinct concern and the ticket specifies the main file.
**Confidence:** High (ticket is unambiguous on file assignment)

---

### [M25-04] SPECIFICATION — `player_slime` exclusion coverage
**Would have asked:** The ticket says `player_slime` is excluded because it is not in `AnimatedEnemyBuilder.ENEMY_CLASSES`. The spec must state whether `player_slime` silently returns no rotation keys, or whether `options_for_enemy("player_slime", ...)` raises an error, or returns empty/default.
**Assumption made:** `player_slime` is present in `animated_build_controls_for_api()` output (via the `| {"spider", "player_slime"}` union in the slugs set) but `_rig_rotation_control_defs()` will be inserted unconditionally for ALL slugs in that loop, which means player_slime WILL receive the rotation defs too. The test requirement "player_slime does not contain RIG_HEAD_ROT_X" contradicts the insertion strategy unless rotation defs are conditionally excluded for player_slime. Spec conservatively mandates: rotation defs appear for all slugs in ENEMY_CLASSES only (i.e., the 6 animated enemies); the insertion in `animated_build_controls_for_api()` is guarded by `if slug in AnimatedEnemyBuilder.ENEMY_CLASSES`. This matches the ticket's AC "all animated slugs that expose explicit head/body parts".
**Confidence:** Medium (the ticket body is ambiguous on unconditional vs conditional insertion; this log records the conservative interpretation)

---

### [M25-04] SPECIFICATION — inf handling for rotation values
**Would have asked:** The validate module treats NaN by reverting to default, but does not explicitly mention inf. The ticket says "NaN/inf behavior (coerce to 0.0 default, same pattern as texture controls)". The existing `_sanitize_zone_geometry_extras` handles inf specially for shell_scale but other floats do not.
**Assumption made:** For rotation keys, both NaN and inf are coerced to the control default (0.0) before the clamp, matching the existing `coerce_validate_enemy_build_options` float branch behavior where NaN reverts to default. Inf: `float("inf") > 180.0` so the clamp `max(lo, min(hi, v))` naturally caps it to 180.0 without a special inf check. Spec states: NaN → revert to default 0.0; inf → clamped to ±180.0 by the standard clamp (no separate inf guard needed). This is the conservative minimal change.
**Confidence:** High (inf is handled implicitly by clamp; only NaN needs explicit handling, already in the validate loop)
