# M11-12 Planner Run — 2026-05-25

## Ticket
`project_board/11_milestone_11_base_mutation_attacks/in_progress/12_verify_cooldown_behavior.md`

## Stage
PLANNING → SPECIFICATION

## Execution Plan

### Analysis Summary

Cooldown logic lives in `_tick_controller_timers()` (player_controller_3d.gd:229-235). It decrements ALL entries in `_mutation_cooldowns` by `delta` on **every physics frame** — unconditionally of player state. This means:

- Cooldown continues during IDLE, WALK, JUMP, FALL, FLOAT, WALL_CLING — confirmed by code path.
- Cooldown continues during HURT — same code path; no state gate.
- Cooldown does **NOT** reset on DEAD — `reset_hp()` (line 765) resets the state machine and clears acid dots but does NOT clear `_mutation_cooldowns`.

### Design Decision (Checkpoint)

**Would have asked:** Should `reset_hp()` (death/respawn) clear `_mutation_cooldowns`?
**Assumption made:** Yes — the ticket's AC states "Die and respawn, verify cooldown resets." The spec must declare that `reset_hp()` clears cooldowns. Implementation will add `_mutation_cooldowns.clear()` to `reset_hp()`.
**Confidence:** High (ticket AC is explicit)

### Tasks Decomposed

| # | Task Objective | Assigned Agent | Dependencies | Estimated Effort |
|---|---|---|---|---|
| 1 | Write spec: cross-state cooldown behavior contract | Spec Agent | None | Small (1 spec) |
| 2 | Design integration tests for cross-state cooldown continuity | Test Designer | Task 1 | Medium (1 test file, ~6 test cases) |
| 3 | Adversarial test pass: edge cases and mutation testing | Test Breaker | Task 2 | Small (2-4 additional cases) |
| 4 | Implement cooldown reset on death + ensure tests pass | Implementation (Generalist) | Task 3 | Small (1-line fix + test green) |
| 5 | Acceptance criteria gatekeeper verification | AC Gatekeeper | Task 4 | Small |

### Outcome
Plan written. Ticket advanced to SPECIFICATION. Next: Spec Agent.
