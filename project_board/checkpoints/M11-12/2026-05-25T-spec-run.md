# M11-12 Spec Run — 2026-05-25

## Ticket
`project_board/11_milestone_11_base_mutation_attacks/in_progress/12_verify_cooldown_behavior.md`

## Stage
SPECIFICATION → TEST_DESIGN

## Summary

Produced comprehensive spec for cooldown cross-state behavior (CDB-1 through CDB-5). Code inspection confirmed:
- Cooldown decrement is already state-independent (unconditional in `_tick_controller_timers`)
- Cross-state continuity already works (no state-gating logic exists)
- Per-mutation independence already works (dictionary key separation)
- Rapid input rejection already works (`_mutation_cooldowns.get(key, 0.0) > 0.0` check)
- **ONE implementation gap found:** `reset_hp()` does NOT clear `_mutation_cooldowns`

### Checkpoint Entry

### [M11-12] SPECIFICATION — HURT state cooldown behavior
**Would have asked:** Should cooldown pause during HURT state?
**Assumption made:** Cooldown continues during HURT — player should not be penalized for taking damage. The code already implements this (unconditional decrement). Spec codifies existing behavior.
**Confidence:** High

### [M11-12] SPECIFICATION — DEAD state cooldown ticking before reset_hp()
**Would have asked:** Should cooldowns continue to tick while in DEAD state before reset_hp() is called?
**Assumption made:** Yes — the unconditional tick in `_tick_controller_timers` runs regardless of state. Since `reset_hp()` will clear all cooldowns anyway, ticking during DEAD is harmless. Not worth adding a state check.
**Confidence:** High

## Artifact
`project_board/specs/cooldown_cross_state_behavior_spec.md`

## Outcome
Spec complete. 5 requirements (CDB-1 through CDB-5), 24 acceptance criteria. One implementation change required. Ticket advanced to TEST_DESIGN.
