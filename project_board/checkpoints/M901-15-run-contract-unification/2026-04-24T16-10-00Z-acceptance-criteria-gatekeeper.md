# [M901-15-run-contract-unification] ACCEPTANCE_CRITERIA_GATEKEEPER — post-move complete-state confirmation

- **Ticket:** `project_board/901_milestone_901_asset_generation_refactoring/done/15_run_contract_unification.md`
- **Stage found:** `INTEGRATION` (Revision 6) with complete AC evidence and prior folder-consistency hold.
- **Decision:** Set `Stage: COMPLETE`, increment revision to `7`, route to `Human` with `Status: Proceed`.

## Evidence revalidation

- AC evidence remains explicit and unchanged from prior gate review:
  - shared Python run-contract module introduced,
  - backend run router delegates command/env/output prediction behavior,
  - command support includes `animated|player|level|smart|stats|test`,
  - `/api/run/stream` and `/api/run/complete` parity preserved,
  - regression coverage includes command parity and variant-index behavior.
- Prior checkpoint/test evidence remains sufficient and objective for closure.

## Gatekeeper outcome

- Stage-folder consistency requirement is now satisfied because ticket is in milestone `done/`.
- No unresolved blocking issues remain in workflow state.
- Ticket is eligible to be treated as complete without AC/spec edits.
