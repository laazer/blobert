### [M901-15-run-contract-unification] TEST_BREAK — unknown command fail-closed contract behavior
**Would have asked:** Should unknown commands be rejected at the shared run-contract layer itself, or only by router allowlist checks?
**Assumption made:** The shared run-contract API must fail closed independently and raise `ValueError` for unknown commands so routers cannot accidentally bypass safety checks.
**Confidence:** Medium

### [M901-15-run-contract-unification] TEST_BREAK — explicit delegation seam visibility
**Would have asked:** Must the router expose the imported shared `run_contract` module as a stable runtime seam for delegation tests?
**Assumption made:** Yes; tests should pin that seam to prevent regressions that reintroduce private duplicated helper logic in routers.
**Confidence:** Medium
