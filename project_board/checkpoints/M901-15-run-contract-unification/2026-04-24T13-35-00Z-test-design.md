### [M901-15-run-contract-unification] TEST_DESIGN — shared run contract API surface
**Would have asked:** Which exact module path and function names should the shared Python-side run contract expose?
**Assumption made:** Use `src.utils.run_contract` with explicit behavioral functions `build_command`, `prepare_run_environment`, and `predict_output_file` so backend and Python callers can share one deterministic contract seam.
**Confidence:** Medium
