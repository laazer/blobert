# MAINT-SLEEV — sandbox_scene_legacy_external_enemy_visuals

Run: 2026-04-05 — Spec Agent

## Checkpoint

**Outcome**

- Produced `project_board/specs/sandbox_scene_legacy_external_enemy_visuals_spec.md` with requirements **SLEEV-1** through **SLEEV-5**, traceability table to ticket AC, normative new scene path `res://scenes/levels/sandbox/test_movement_3d_legacy_enemy_visual.tscn`, root rename requirement, exact deltas (remove four GLB `ext_resource` + four `model_scene` overrides), and `project.godot` / test-suite non-regression (SLEEV-4, SLEEV-5).
- Documented risk: ticket AC names `run/main_scene` → `test_movement_3d.tscn`; repository baseline may differ — spec requires match or Planner escalation (SLEEV-4.2).

**Would have asked:** None (autonomous; resolved via explicit SLEEV-4.2 escalation path).

**Assumption made:** Source sandbox `test_movement_3d.tscn` as read 2026-04-05 is the duplication baseline (node list, transforms, `mutation_drop`, GLB paths).

**Confidence:** High for structure; medium for `run/main_scene` repo/ticket alignment pending gatekeeper check.

**Log:** `project_board/maintenance/in_progress/sandbox_scene_legacy_external_enemy_visuals.md`
