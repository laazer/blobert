# M902-21 Spec Run

**Run id:** `2026-05-20T-spec-run`  
**Ticket:** `project_board/902_milestone_902_agent_predictabilitiy_improvements/01_in_progress/21_context_budget_tracking.md`  
**Stage:** SPECIFICATION → TEST_DESIGN  
**Agent:** Spec Agent

---

## Outcome

Specification authored at `project_board/specs/902_21_context_budget_tracking_spec.md` (12 requirements, failure taxonomy, test contract). `spec_completeness_check.py --type generic` PASS. Handoff to Test Designer Agent.

---

### [M902-21] SPECIFICATION — context_efficiency formula vs ticket example

**Would have asked:** Should `context_efficiency` match the ticket example value `0.29` exactly or a recomputed ratio?

**Assumption made:** Freeze `context_efficiency = round(output_tokens / total_tokens, 2)`; ticket example for `spec` stage aligns (1840/6090 ≈ 0.30). Add separate `input_efficiency_ratio` for input/schema target < 2.0. Tests enforce spec formulas, not informal example rounding.

**Confidence:** High

---

### [M902-21] SPECIFICATION — schema_size_tokens tokenizer

**Would have asked:** Use tiktoken vs byte heuristic?

**Assumption made:** Compact JSON UTF-8 byte length // 4 for both `schema_size_tokens` and estimation path; document ±25% error bound. No external tokenizer dependency in M902-21.

**Confidence:** High
