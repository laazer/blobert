### [M901-01] TEST_BREAK — router AST scope vs phased migration
**Would have asked:** Should backend `routers/*.py` AST checks (no `sys.path` mutation) be deferred until a separate R4 implementation pass so only `src/` entrypoints are gated first?
**Assumption made:** Encode the full R1+R4 contract in tests now: router files (`assets.py`, `meta.py`, `registry.py`) are in scope per the ticket spec; failures are expected until backend bootstrap is replaced by a packaging-supported layout. Runtime seam test `test_import_backend_registry_pipeline_after_canonical_path_injection` stays green to prove the import pipeline still resolves after `_ensure_python_import_path()`.
**Confidence:** Medium

### [M901-01] TEST_BREAK — subprocess empty-string sys.path entries
**Would have asked:** Is strict list equality on `sys.path` too brittle when `''` appears for cwd semantics in subprocess `-c` runs?
**Assumption made:** Keep strict equality to match in-process contract tests; subprocess uses same PYTHONPATH as `pyproject` (`"."` + `"src"`). Any mismatch is a regression signal for implementation.
**Confidence:** Medium
