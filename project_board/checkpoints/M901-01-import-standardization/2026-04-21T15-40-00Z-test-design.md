### [M901-01-import-standardization] TEST_DESIGN — internal import contract strictness
**Would have asked:** Should internal modules be required to load cleanly from file without package-relative import context, or only when imported through package paths?
**Assumption made:** Enforce the stricter runtime contract: migrated internal modules should rely on canonical absolute imports, so file-based module execution remains import-stable without relative-import dependency.
**Confidence:** Medium

### [M901-01-import-standardization] TEST_DESIGN — package export surface minimum
**Would have asked:** For `__init__.py` API exposure, should tests assert only module re-exports or concrete callable/value symbol re-exports?
**Assumption made:** Enforce concrete symbol-level exports for high-value package APIs (`src.materials` and `src.model_registry`) to prevent partial/implicit export regressions.
**Confidence:** Medium
