# M901-01 import standardization — STATIC_QA (diff-cover remediation)

**Ticket:** `project_board/901_milestone_901_asset_generation_refactoring/ready/01_import_standardization.md`

## Evidence

- Targeted tests: `tests/materials/test_stripes_texture_generation.py`, `test_spots_adversarial.py`, `test_feature_zone_materials.py` — green.
- Full suite: `uv run python -m pytest tests/ -q` in `asset_generation/python` → **2093 passed**, 7 skipped.
- `bash ci/scripts/diff_cover_preflight.sh` → **PASS** — diff vs `origin/main`: **100%** (73 lines, 0 missing); `gradient_generator.py` and `material_system_enemy_themes.py` at 100% on diff.

## Changes

- Behavior tests for `_stripes_texture_generator` input validation (int dimensions, bool rejection, positive size, string hex colors, numeric `stripe_width`).
- Behavior tests for `_spots_texture_generator` dimension/type validation and hex edge cases (double `#`, invalid bg chars with fallback).
- Behavior tests for `get_enemy_materials`: no registered theme → default palette; exactly two theme materials → limbs/joints reuse first material.
