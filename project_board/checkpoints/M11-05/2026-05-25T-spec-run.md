# M11-05 Spec Run — 2026-05-25

**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/05_attack_executor_handlers.md`
**Agent:** Spec Agent
**Stage:** SPECIFICATION → TEST_DESIGN

---

## Discovery Summary

### Codebase evidence reviewed

- `scripts/attacks/attack_resource.gd` — COMPLETE (M11-04, 15 exports, commit `be206a7`).
- `scripts/enemies/enemy_base.gd` — **No `take_damage()` method.** Only state enum + `set_base_state()`.
- `scripts/enemies/enemy_attack_hitbox.gd` — Existing Area3D hitbox pattern with `_compute_knockback()` (zeroes Z, normalizes delta, scales by strength).
- `scripts/combat/acid_projectile_3d.gd` — Enemy-facing projectile (damages player, not enemies). Pattern: `setup(dir, speed)`, `_physics_process` movement, `_on_body_entered` hit detection.
- `scripts/enemy/adhesion_bug_lunge_attack.gd` — Uses `player.has_method("apply_enemy_movement_root")` guard pattern (line 114).
- `scripts/enemy/acid_spitter_ranged_attack.gd` — Enemy projectile spawning pattern (add to parent's parent, preload scene).
- `scripts/player/player_controller_3d.gd` — Has `take_damage(amount, knockback)`, `apply_enemy_movement_root()`, `apply_enemy_acid_damage()`. Player-facing damage API exists.
- `project_board/specs/mutation_attack_system_design_spec.md` — Design spec with handler pseudocode and dispatch pattern.

### Discrepancy resolutions

Six discrepancies resolved (AEX-DR-1 through AEX-DR-6):
1. Enemy `take_damage()` missing → duck-type `has_method()` guard; mock in tests; defer real method.
2. Modifier methods missing → same `has_method()` guard pattern; matches existing `AdhesionBugLungeAttack` codebase pattern.
3. Startup frames → timer-based (`create_timer(frames / 60.0)`), not literal frame counting.
4. Player projectile → new `PlayerProjectile3D` class (separate from enemy-facing `AcidProjectile3D`).
5. VFX/SFX → signal-based stubs only (four signals declared).
6. `knockback_direction` → top-level AttackResource property per frozen ATK spec.

---

## Assumptions Logged

### [M11-05] SPECIFICATION — _query_enemies_in_range implementation
**Would have asked:** Should `_query_enemies_in_range()` use physics raycasting (PhysicsDirectSpaceState3D) or group-based distance filtering (`get_nodes_in_group + distance check`)?
**Assumption made:** Spec defines the contract (center + radius → array of Node3D in range) but leaves implementation approach to the Implementation Agent. Both approaches are valid; group-based is simpler for unit testing.
**Confidence:** High

### [M11-05] SPECIFICATION — Projectile scene creation
**Would have asked:** Should the spec define the `.tscn` scene file structure for `PlayerProjectile3D`, or defer to implementation?
**Assumption made:** Spec defines only the script interface (`PlayerProjectile3D` extends `Area3D`, with damage/speed/lifetime/modifiers properties). Scene file creation is an implementation detail.
**Confidence:** High

### [M11-05] SPECIFICATION — push_warning vs push_error for unknown effect_type
**Would have asked:** Should unknown effect_type be `push_error` (as in design spec) or `push_warning`?
**Assumption made:** `push_warning` — unknown types are an extensibility concern during development, not a fatal runtime error. New effect_types may not have handlers yet.
**Confidence:** Medium

---

## Spec Output

**File:** `project_board/specs/attack_executor_spec.md`
**Requirements:** AEX-1 through AEX-8
**Discrepancy resolutions:** AEX-DR-1 through AEX-DR-6
**Edge cases:** EC-1 through EC-20
**Frozen API surface:** 4 signals, 2 public methods, 8 internal methods
**PlayerProjectile3D interface:** 7 properties (damage, speed, lifetime, knockback_magnitude, knockback_direction, modifiers, direction_x)
**Deferred boundary:** 11 items out-of-scope
**Test strategy:** 20+ test categories across primary + adversarial suites

---

## Outcome

SPECIFICATION COMPLETE. Stage → TEST_DESIGN. Handoff to Test Designer Agent.
