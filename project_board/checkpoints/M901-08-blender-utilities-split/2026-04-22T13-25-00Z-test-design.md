### [M901-08-blender-utilities-split] TEST_DESIGN — runtime contract seams for split
**Would have asked:** Should split validation focus on structural/prose checks or executable runtime seams?
**Assumption made:** Enforce runtime seams only: importability, re-export identity, canonical module ownership, fallback dispatch behavior, deterministic helpers, and bind fail-safe flow.
**Confidence:** High

### [M901-08-blender-utilities-split] TEST_DESIGN — local test runner availability
**Would have asked:** Which Python environment should be used when the default interpreter cannot run pytest?
**Assumption made:** Author tests and capture local blocker (`python3 -m pytest ...` fails with `No module named pytest`) for downstream execution in configured environment.
**Confidence:** High
