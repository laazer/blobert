### [M9-02] TEST_BREAK — stripe_width type coercion
**Would have asked:** Should `_stripes_texture_generator` reject non-numeric `stripe_width` values instead of coercing via `float()`?
**Assumption made:** Conservative behavior is strict rejection for non-numeric caller input; encoded in a failing adversarial test with `# CHECKPOINT`.
**Confidence:** Medium

### [M9-02] TEST_BREAK — NaN/Inf stripe rotation handling
**Would have asked:** For stripe rotation inputs, should NaN/Inf be hard-failed or sanitized?
**Assumption made:** Conservative runtime stability choice is sanitize NaN/Inf to `0.0` before factory handoff; encoded in adversarial integration test with `# CHECKPOINT`.
**Confidence:** High
