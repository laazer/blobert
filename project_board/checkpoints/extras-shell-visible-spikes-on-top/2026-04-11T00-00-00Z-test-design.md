### [extras-shell-visible-spikes-on-top] TEST_DESIGN — shell location tuple vs keyword arg

**Would have asked:** Does `create_sphere` receive `location` as a positional tuple argument or as a keyword argument `location=`? The bulb path in `_append_body_ellipsoid_extras` passes `location=p` where `p` is a tuple, not a keyword `location=(cx,cy,cz)`. For shell, the spec says `location=(cx,cy,cz)`.
**Assumption made:** Tests capture both `args[0]` (positional) and `kwargs.get("location")` to extract the location, matching the same pattern used by bulb calls. Strict tests check keyword form matching the spec's documented call signature.
**Confidence:** High

---

### [extras-shell-visible-spikes-on-top] TEST_DESIGN — spike protrusion test with factor=0.55 in current code

**Would have asked:** Should `test_spike_tip_protrudes_above_surface` assert the post-fix behavior (factor=1.0, cone base flush on surface) or the current behavior (factor=0.55, cone base embedded)?
**Assumption made:** The test encodes the desired post-fix behavior. The observable invariant is: the cone BASE (at `tip - depth * nrm`) must not be embedded — its distance from the zone center must be >= surface radius. For a unit sphere: base_dist = 1 + depth * (factor - 1); with factor=0.55 this is 1 - 0.45*depth < 1.0 (FAILS test); with factor=1.0 this is 1.0 (PASSES). Initial draft tested tip distance > 1.0 which passes even for factor=0.55; corrected to test base distance >= 1.0. The final test encodes the correct invariant and fails against current 0.55 factor code.
**Confidence:** High

---

### [extras-shell-visible-spikes-on-top] TEST_DESIGN — shell_scale field not yet in `_default_zone_geometry_extras_payload`

**Would have asked:** `shell_scale` is NOT yet in `_default_zone_geometry_extras_payload()`, `_ZONE_GEOM_EXTRA_FIELDS`, `_EXTRA_ZONE_FLAT_KEY`, or `_zone_extra_control_defs`. Should the options tests assert the pre-implementation state (field absent) or the post-implementation state (field present)?
**Assumption made:** Tests assert the post-implementation state (field present, regex matches, control def exists). These are failing tests by design, to be satisfied by the Implementation Agent.
**Confidence:** High

---

### [extras-shell-visible-spikes-on-top] TEST_DESIGN — shell branch not yet in attach module

**Would have asked:** `_append_body_ellipsoid_extras` and `_append_head_ellipsoid_extras` do not yet have an `elif kind == "shell":` branch. Do the attach tests for shell need to be marked xfail or just written as failing?
**Assumption made:** Tests are written as straightforward assertions that will fail until implementation. No xfail marker is used — the pipeline expects them to fail at TEST_BREAK stage and pass after implementation.
**Confidence:** High

---

### [extras-shell-visible-spikes-on-top] TEST_DESIGN — AC-SS8 control def ordering

**Would have asked:** The spec says shell_scale control def appears in `_zone_extra_control_defs` but does not specify exact ordering relative to offset_x/y/z or hex. The offset test `test_zone_extra_control_defs_offset_xyz_after_hex` asserts offset comes after hex. Should shell_scale also come after hex?
**Assumption made:** Per spec: "One entry per zone, following the same per-zone loop pattern as spike_size and bulb_size control defs." Test asserts shell_scale def is present in the per-zone defs with correct shape (type=float, min=1.01, max=1.5, step=0.01, default=1.08). No strict ordering assertion beyond presence is made.
**Confidence:** High
