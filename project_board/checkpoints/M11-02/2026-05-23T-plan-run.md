# M11-02 Planning Run

**Run id:** `2026-05-23T-plan-run`  
**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/02_physics_frame_order.md`  
**Stage:** PLANNING → SPECIFICATION  
**Planner:** Planner Agent

---

## Outcome

Planning complete. Eleven-task execution plan embedded in ticket (revision 2). M11-01 prerequisite satisfied. Greenfield: frame-order spec, jump buffer, one-way collision layers, and player frame-order tests. Handoff to Spec Agent.

---

## Repo Discovery (Planning Evidence)

| Asset | Path | Relevance |
|-------|------|-----------|
| Player controller | `scripts/player/player_controller_3d.gd` | `_physics_process`: FSM `update` early; `simulate` then `move_and_slide` mid-frame; `sync_from_context` after slide |
| Kinematic sim | `scripts/movement/movement_simulation.gd` | Coyote in `simulate()`; extensive tests in `tests/scripts/movement/test_jump_simulation*.gd` |
| Gameplay FSM | `scripts/player/player_state_machine.gd` | M11-01 complete; `update` / `sync_from_context` wired |
| Visual feedback | `scripts/fx/slime_visual_state.gd` | `_process` pull from parent — not controller push |
| Animation | `scripts/player/player_export_animation_controller_3d.gd` | `_physics_process` reads `CharacterBody3D` floor/velocity |
| Physics layers | `project.godot` | No `[layer_names]` / one-way bit declared |
| Frame-order tests | `tests/**/*frame*order*` | **None** |
| Upstream ticket | `done/01_player_state_machine.md` | Satisfied |

**Current `_physics_process` order (abbreviated):** input → `state_machine.update` → floor/wall snapshot → `simulate` → velocity assign → **`move_and_slide`** → state writeback → chunks → **`sync_from_context`**.

**Target order (ticket AC):** FSM update → timers → dispatch → collision mask → renderer sync → **`move_and_slide`**.

---

### [M11-02] PLANNING — Coyote in sim vs controller timers

**Would have asked:** Should coyote timer move out of `MovementSimulation` into controller step 2?

**Assumption made:** Keep coyote decrement/reset inside `simulate()` (step 3 “state handler”) unless spec proves buffer ordering requires controller-owned coyote. Spec documents both timers: jump buffer at controller (new), coyote in sim (existing). Controller may synthesize effective `jump_just_pressed` from buffer before calling `simulate()`.

**Confidence:** High

---

### [M11-02] PLANNING — One-way platforms without layer_names

**Would have asked:** Defer one-way AC until level content exists?

**Assumption made:** Spec defines Godot 3D physics layer bit for ONE_WAY + player mask constants; implementation adds `project.godot` layer names and a minimal sandbox test fixture (e.g. extend `test_movement_3d` or dedicated scene). AC requires pass-through-from-below / land-from-above behavior in tests, not only prose.

**Confidence:** Medium

---

### [M11-02] PLANNING — Renderer sync scope

**Would have asked:** Does “renderer sync” mean reorder child `_physics_process` or explicit controller push?

**Assumption made:** Spec mandates controller-initiated read-only sync after kinematic state is committed and before `move_and_slide()`. Implementation may add `_sync_renderer_from_state()` calling into `SlimeVisualState` / animation driver; eliminate mid-frame stale reads where feasible without rewriting GLB import tree.

**Confidence:** Medium

---

### [M11-02] PLANNING — Branch test suite green

**Would have asked:** Block M11-02 until M11-01 unrelated failures fixed?

**Assumption made:** SPECIFICATION and test design proceed. COMPLETE remains blocked until `timeout 300 ci/scripts/run_tests.sh` exits 0 (documented WARN from M11-01 gatekeeper: UI + test_utils failures). Not a planning blocker.

**Confidence:** High

---

### [M11-02] PLANNING — Spec path

**Would have asked:** `agent_context/agents/2_spec/` vs `project_board/specs/`?

**Assumption made:** `agent_context/agents/2_spec/player_physics_frame_order_spec.md` per `PATTERN_MAPPING_AUDIT.md` and M11-01 convention. Spec exit gate: `generic`.

**Confidence:** High
