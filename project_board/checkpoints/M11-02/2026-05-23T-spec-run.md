# M11-02 Spec Agent Run — 2026-05-23

**Run id:** `2026-05-23T-spec-run`  
**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/02_physics_frame_order.md`  
**Stage:** SPECIFICATION → TEST_DESIGN

---

## Outcome

- Spec: `project_board/specs/player_physics_frame_order_spec.md` (revision 1, type `generic`)
- Ticket stage: `TEST_DESIGN`, revision 3
- Handoff: `handoff-latest.yaml` (spec → test_designer)

---

## Frozen Decisions

| Topic | Decision |
|-------|----------|
| Spec path | `project_board/specs/player_physics_frame_order_spec.md` (repo convention; planner `agent_context/` path superseded) |
| Pipeline | 10-step PFO-2 contract: input → FSM update → controller timers → collision read → dispatch sim → one-way mask → renderer sync → move_and_slide → post-slide housekeeping → FSM sync |
| Jump buffer | Net-new controller `@export jump_buffer_time = 0.1`; effective press into `simulate()` |
| Coyote | Stays in `MovementSimulation`; controller mirrors export only (PFO-4) |
| One-way layers | Bit 1=`ground`, bit 2=`one_way`; mask `3` descending, `1` ascending |
| Test fixture | `scenes/levels/sandbox/test_one_way_platform_3d.tscn` |
| I-frames | Deferred — reserved comment in Step 2 only |
| Land squash | Post-slide Step 8 (collision unknowable pre-slide) |
| Jump stretch | Pre-slide Step 6 on sim jump rising edge |
| M11-01 hooks | `update()` Step 1 once; `sync_from_context` Step 9 after slide |

---

### [M11-02] SPECIFICATION — Spec file location

**Would have asked:** Use `agent_context/agents/2_spec/player_physics_frame_order_spec.md` or `project_board/specs/`?

**Assumption made:** Follow repo convention (`project_board/specs/`). Handoff evidence points to `project_board/specs/player_physics_frame_order_spec.md`.

**Confidence:** High

---

### [M11-02] SPECIFICATION — Coyote timer ownership

**Would have asked:** Move coyote decrement to controller Step 2 per ticket prose?

**Assumption made:** Keep coyote in `MovementSimulation` (M1 sim tests authoritative); controller Step 2 only jump buffer.0.1s. Ticket execution plan aligns.

**Confidence:** High

---

### [M11-02] SPECIFICATION — I-frames in Step 2

**Would have asked:** Implement invulnerability timers now?

**Assumption made:** Defer — no iframe implementation exists; Step 2 reserved comment only. Out of scope per Deferred Boundary.

**Confidence:** High

---

### [M11-02] SPECIFICATION — One-way layer greenfield

**Would have asked:** Which physics layer bits are already assigned?

**Assumption made:** No `layer_names` in `project.godot` today; freeze bit 1=ground, bit 2=one_way; player baseline mask 3.

**Confidence:** Medium

---

### [M11-02] SPECIFICATION — Land squash vs renderer sync order

**Would have asked:** Must land squash run before `move_and_slide()`?

**Assumption made:** Land squash stays post-slide (Step 8); Step 6 renderer sync uses post-dispatch kinematic intent only. Ticket AC "after ALL game state updates" means post-sim/timers/mask, not post-collision.

**Confidence:** Medium

---

## Orchestrator Follow-ups

```bash
python ci/scripts/spec_completeness_check.py project_board/specs/player_physics_frame_order_spec.md --type generic
python ci/scripts/run_workflow_transition_gates.py --ticket-id M11-02 --transition spec_to_test_design
```

## Spec Exit Gate

```
spec-completeness-check: player_physics_frame_order_spec.md  type=generic
PASS: all required sections present.
```

## Workflow Transition Gates

```
transition=spec_to_test_design ticket_id=M11-02
  todo_validation_check: PASS — All todos completed for Spec Agent.
  handoff_validation_check: PASS — Handoff checklist valid for spec→test_designer.
```
