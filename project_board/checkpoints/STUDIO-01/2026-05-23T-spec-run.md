# STUDIO-01 Spec Agent Run — 2026-05-23

**Run id:** `2026-05-23T-spec-run`  
**Ticket:** `project_board/43_milestone_43_studio_editor_redesign/in_progress/STUDIO-01_studio_shell_tokens.md`  
**Stage:** SPECIFICATION → TEST_DESIGN

---

## Outcome

- Spec: `project_board/specs/studio_editor_redesign_spec.md` (STUDIO-01 freeze revision 2)
- Ticket stage: `TEST_DESIGN`, revision 3
- Handoff: `handoff-latest.yaml` (spec → test_designer)

---

## Frozen Decisions

| Topic | Decision |
|-------|----------|
| Phase 1 scope | §6–§9, §10, §12, §14–§16 only; Phases 2–4 unchanged deferred |
| Studio center column | `PreviewSourceBar` + `GlbViewer` + `AnimationControls` only |
| CommandPanel / Terminal | **Not** mounted in Studio center (or any Studio column) in Phase 1; deferred STUDIO-02+ per §12 |
| Legacy layout test id | `data-testid="legacy-layout"` on `ThreePanelLayout` when flag off (FR-1) |
| Preview column test id | `data-testid="studio-preview-column"` (FR-4) |
| Invalid flag | Any value other than exactly `"1"` → legacy layout (§11) |
| Hydration | No `selectAssetByPath({ importBuildOptions: true })` from Studio subtree (FR-6, §10) |
| Inspector | Five placeholder tabs only; no `ColorsPane` / registry wiring (FR-5, §12) |
| Tests | T-1..T-6 in §8 (added T-6 for CommandPanel/Terminal absence when flag on) |

---

### [STUDIO-01] SPECIFICATION — CommandPanel/Terminal in Studio center

**Would have asked:** Should Phase 1 Studio center include CommandPanel/Terminal like legacy `ThreePanelLayout`?

**Assumption made:** No — Phase 1 matches prototype IA (preview hero + animation rail). Legacy layout retains CommandPanel/Terminal. Explicit FR-4 exclusion + §12 deferral + T-6.

**Confidence:** High

---

### [STUDIO-01] SPECIFICATION — Top bar Save/Regenerate vs CommandPanel

**Would have asked:** Wire top-bar actions to CommandPanel handlers in STUDIO-01?

**Assumption made:** Defer to Phase 2 (existing spec §4 top bar note). No CommandPanel mount in Studio for STUDIO-01.

**Confidence:** High

---

## Orchestrator Follow-ups

```bash
python ci/scripts/spec_completeness_check.py project_board/specs/studio_editor_redesign_spec.md --type generic
python ci/scripts/run_workflow_transition_gates.py --ticket-id STUDIO-01 --transition spec_to_test_design
```

## Spec Exit Gate

```
spec-completeness-check: studio_editor_redesign_spec.md  type=generic
PASS: all required sections present.
```

## Workflow Transition Gates

```
transition=spec_to_test_design ticket_id=STUDIO-01
  todo_validation_check: PASS — All todos completed for Spec Agent.
  handoff_validation_check: PASS — Handoff checklist valid for spec→test_designer.
```
