# STUDIO-01 Planning Run

**Run id:** `2026-05-23T-plan-run`  
**Ticket:** `project_board/43_milestone_43_studio_editor_redesign/in_progress/STUDIO-01_studio_shell_tokens.md`  
**Stage:** PLANNING → SPECIFICATION  
**Planner:** Planner Agent

---

## Outcome

Planning complete. Ten-task execution plan embedded in ticket and `project_board/execution_plans/STUDIO-01_studio_shell_tokens.md`. No gating child dependencies (STUDIO-02+ deferred). Pre-authored spec at `project_board/specs/studio_editor_redesign_spec.md` — Spec Agent validates §6–§9 only. Handoff to Spec Agent.

---

## Repo Discovery (Planning Evidence)

| Asset | Path | Relevance |
|-------|------|-----------|
| App entry | `asset_generation/web/frontend/src/App.tsx` | Always renders `ThreePanelLayout`; no flag branch yet |
| Legacy layout | `asset_generation/web/frontend/src/components/layout/ThreePanelLayout.tsx` | Preview stack: `PreviewSourceBar`, `GlbViewer`, `AnimationControls` + CommandPanel/Terminal |
| Layout tests | `ThreePanelLayout.preview_collapse.test.tsx`, `ThreePanelLayout.extras_tab.test.tsx` | Mock patterns for Vitest suite |
| Store hydration | `useAppStore.selectAssetByPath` + `useAppStore.selectAssetByPath.previewOnly.test.ts` | REQ-1: no implicit `importBuildOptions` from preview |
| Element prototype | `bot_vault/asset_generation/redesign_v1/shared.jsx` | Nine elements with hue/soft/ink |
| Design reference | `bot_vault/asset_generation/redesign_v1/studio.jsx` | IA only — not 1:1 port |
| Authoritative spec | `project_board/specs/studio_editor_redesign_spec.md` | Phase 1 frozen §6–§9 |
| Studio components | — | **Absent** (greenfield) |

**Test ids today:** Neither `studio-layout` nor `legacy-layout` exist — Implementation must add per NFR-4 / AC-1–AC-2.

---

### [STUDIO-01] PLANNING — Pre-authored spec vs new spec file

**Would have asked:** Should Spec Agent write a new spec under `agent_context/agents/2_spec/` or validate the existing `project_board/specs/studio_editor_redesign_spec.md`?

**Assumption made:** Validate and finalize the existing milestone spec (§6–§9 scope only). Spec exit gate `--type generic`. No duplicate spec file unless completeness check finds gaps.

**Confidence:** High

---

### [STUDIO-01] PLANNING — CommandPanel / Terminal in Studio center column

**Would have asked:** Legacy preview column includes `CommandPanel` and `Terminal` below animations. Should Studio Phase 1 include them?

**Assumption made:** **Omit** from `StudioPreviewColumn` in STUDIO-01. Spec §4 and §79 center column lists only `PreviewSourceBar`, `GlbViewer`, `AnimationControls`. Command/terminal wiring deferred to STUDIO-02+ or inspector integration. Spec Agent confirms in Task 1.

**Confidence:** Medium

---

### [STUDIO-01] PLANNING — Fonts (Plus Jakarta Sans)

**Would have asked:** Add web fonts in STUDIO-01 or defer?

**Assumption made:** Optional per spec §13 — system/Segoe stack acceptable until Phase 2. No font blocking for shell landing.

**Confidence:** High

---

### [STUDIO-01] PLANNING — Umbrella / child dependency gate

**Would have asked:** Does STUDIO-01 block on STUDIO-02 or hydration bugfix completion?

**Assumption made:** No umbrella ticket. STUDIO-02+ explicitly deferred (§12). Hydration bugfix is an invariant to preserve, not a gating dependency. Stage advances to SPECIFICATION.

**Confidence:** High

---
