# Checkpoint Log: M12-02 Spec Run
**Ticket:** `project_board/12_milestone_12_fused_mutation_attacks/backlog/02_fused_attack_resources.md`
**Run ID:** 2026-05-29T-spec-run
**Stage:** SPECIFICATION
**Agent:** Spec Agent
**Date:** 2026-05-29

---

## Summary

Produced complete functional and non-functional specification for M12-02 (Fused Attack Resources). Froze all 4 critical design decisions from the planning checkpoint, defined all 6 fused attack stat blocks at values consistent with base attack conventions, and specified the named-constant pattern that satisfies `gd-review`.

---

## Checkpoint Assumptions

### [M12-02] SPECIFICATION — Attack names for fused combos
**Would have asked:** What flavor names should the 6 fused attacks have? The ticket gave "Toxic Slash" and "Corrosive Shell" as examples for two combos. Should the spec freeze the names?
**Assumption made:** Yes — the spec freezes all 6 attack names as narrative flavor strings. They do not need to follow the sorted-key order. Names chosen to reflect the fusion identity of both components. Names become named constants (e.g., `ACID_CLAW_NAME`). Implementation must use them verbatim.
**Confidence:** High

### [M12-02] SPECIFICATION — vfx_scale and startup_frames for fused attacks
**Would have asked:** The base attacks use vfx_scale 1.0–1.5 and startup_frames 0–12. Should fused attacks differ on these?
**Assumption made:** Fused attacks use vfx_scale slightly elevated (1.2–1.8) compared to base counterparts to give them a distinct visual identity, within the range established by existing base attacks. startup_frames for MELEE_SWIPE fused = 0 (same as Claw), for SLAM_AOE fused = 8 (less than Carapace's 12 to distinguish), for PROJECTILE_SPIT fused = 0 (same as Acid/Adhesion). The spec freezes all values.
**Confidence:** High

### [M12-02] SPECIFICATION — projectile_lifetime for fused projectile attacks
**Would have asked:** Base projectile attacks use projectile_lifetime 2.0 (Acid) and 1.25 (Adhesion). What should fused projectile combos use?
**Assumption made:** Fused projectile combos use a lifetime between their base components. Specific values frozen per-combo in stat blocks below. Conservative choice: values that stay within the 1.0–2.5s range already used by base attacks.
**Confidence:** Medium

### [M12-02] SPECIFICATION — attack_range for projectile fused attacks
**Would have asked:** Base PROJECTILE_SPIT attacks use attack_range 0.0 (range is implicit in projectile_lifetime * projectile_speed). Should fused projectile attacks also use 0.0?
**Assumption made:** Yes — PROJECTILE_SPIT fused attacks use attack_range 0.0 following the same convention as Acid (attack_range 0.0) and Adhesion (attack_range 0.0). Effective range is determined by projectile_lifetime * projectile_speed.
**Confidence:** High

---

## Files Produced

- `project_board/specs/fused_attack_resources_spec.md`

---

## Stat Block Derivation Notes

Base attack stat ranges observed in `attack_database.gd`:

| Stat | Claw | Acid | Carapace | Adhesion |
|------|------|------|----------|----------|
| damage | 3.0 | 1.0 | 4.0 | 1.0 |
| cooldown | 0.8 | 2.0 | 3.5 | 2.5 |
| attack_range | 1.5 | 0.0 | 3.0 | 0.0 |
| knockback_magnitude | 2.0 | 0.0 | 5.0 | 0.0 |
| projectile_speed | 0.0 | 8.0 | 0.0 | 8.0 |
| projectile_lifetime | 0.0 | 2.0 | 0.0 | 1.25 |

Fused stat ranges used in this spec (always within or modestly above base):
- damage: 2.0–5.5 (fused is stronger than both components, capped below 6.0)
- cooldown: 1.0–4.0 (longer than either component, represents combined weight)
- knockback_magnitude: 0.0–8.0 (claw combos escalate from 2.0; carapace combos escalate from 5.0)
- projectile_speed: 8.0–14.0 (base is 8.0; fused may be slightly faster)
- attack_range: 0.0 (projectile) or 1.5–3.5 (melee/aoe)
