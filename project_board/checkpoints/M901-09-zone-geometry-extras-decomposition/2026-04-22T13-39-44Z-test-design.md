### [M901-09-zone-geometry-extras-decomposition] TEST_DESIGN — decomposition contract API assumptions
**Would have asked:** Should extracted module helper names be strictly frozen, or can tests bind by semantic callable role while asserting required behavior?
**Assumption made:** Use semantic callable-role resolution (tokenized name discovery) for extracted helpers so tests enforce runtime behavior contracts without over-constraining internal naming.
**Confidence:** Medium

### [M901-09-zone-geometry-extras-decomposition] TEST_DESIGN — local test runner availability
**Would have asked:** Which Python environment should execute pytest for local evidence?
**Assumption made:** Author RED contract tests and record local execution blocker because `python3 -m pytest` fails with `No module named pytest`; downstream agent should run in configured project test environment.
**Confidence:** High
