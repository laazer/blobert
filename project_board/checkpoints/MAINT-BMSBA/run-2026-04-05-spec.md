### [MAINT-BMSBA] Spec — base_models_split_by_archetype

**Would have asked:** Whether to keep a thin `base_models.py` shim file instead of a package-only layout; whether `get_available_types()` order must be explicitly specified.

**Assumption made:** Package `base_models/` with `__init__.py` re-exports (no coexisting `base_models.py`); `get_available_types` must remain `['insectoid', 'blob', 'humanoid']` in that order per current dict literal order.

**Confidence:** High
