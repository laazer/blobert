# M902-12 Checkpoint: Test Fix — Weight-Based Band Classification

**Date:** 2026-05-19  
**Agent:** Test Designer Agent  
**Task:** Update test assertions to match weight-based band classification (spec v1.1 correction)

## Issue Resolved

Spec v1.1 clarified that band thresholds apply to the WEIGHT scale [0-20], not the SCORE scale [0-100]:
- Weight 0-2 → EXIT
- Weight 3-5 → WARN
- Weight 6+ → ESCALATE

Implementation was already correct (uses weight-based classification). Test assertions were outdated and expected band values per (incorrect) score-based mapping.

## Test Corrections Applied

### File: `tests/ci/test_risk_scoring_check.py`

**TV-02 (line 113-119):** Single SRP violation
- Old: `assert result["band"] == "EXIT"` (weight=3, incorrect)
- New: `assert result["band"] == "WARN"` (weight=3, correct per spec v1.1)
- Docstring updated: "risk_score=15, band=WARN"

**TV-03 (line 121-128):** Single duplication violation
- Old: `assert result["band"] == "WARN"` (weight=1, incorrect)
- New: `assert result["band"] == "EXIT"` (weight=1, correct per weight-based rule)
- Docstring updated: "risk_score=5, band=EXIT"

**TV-04 (line 130-140):** Low-risk mixed
- Old: `assert result["band"] == "WARN"` (weight=2, incorrect)
- New: `assert result["band"] == "EXIT"` (weight=2, correct per weight-based rule)
- Docstring updated: "risk_score=10, band=EXIT"

**TV-09 (line 184-194):** Two SRP violations
- Old: `assert result["band"] == "WARN"` (weight=6, incorrect)
- New: `assert result["band"] == "ESCALATE"` (weight=6, correct per weight-based rule)
- Docstring updated: "risk_score=30, band=ESCALATE"

**TV-14 (line 221-236):** All 8 signals
- Issue: Test was missing migration signal (needed weight=20 for risk_score=100)
- Fix: Added DUP-02 violation with file path "alembic/versions/001_migration.py" to trigger migration detection (+2)
- Now: weight=20, risk_score=100, band=ESCALATE (all 8 signals included)
- Comment updated: "3+5+1+5+2+1+1+2 = 20"

**test_band_escalate_six (line 365-379):**
- Old: `assert result["risk_score"] == 30` with comment "# 1+1+1+1+1+2 = 7, (7/20)*100 = 35"
- New: `assert result["risk_score"] == 35` (actual value, matches weight=7)
- Docstring updated: "risk_score=30 → band=ESCALATE" to "risk_score=35 → band=ESCALATE"

**test_output_recommendation_matches_band (line 534-553):**
- Old: violations_warn = [DUP-01, OB-01] (weight=2 → EXIT, but test expected WARN)
- New: Added OB-02 to violations_warn: [DUP-01, OB-01, OB-02] (weight=3 → WARN)
- Ensures test correctly validates band=WARN with recommendation=medium_risk_review

**test_low_risk_pattern_duplication_only (line 690-699):**
- Old: `assert result["band"] == "WARN"` (DUP-01 weight=1 → should be EXIT)
- New: `assert result["band"] == "EXIT"` (correct per weight-based rule)

**test_medium_risk_pattern_duplication_plus_complexity (line 712-721):**
- Old: violations = [DUP-01, OB-01] (weight=2 → EXIT, but test expected WARN)
- New: Added OB-02: [DUP-01, OB-01, OB-02] (weight=3 → WARN)
- Ensures "medium-risk" label is accurate (weight >= 3)

### File: `tests/ci/test_risk_scoring_check_adversarial.py`

**test_consistency_recommendation_warn_with_warn_band (line 925-935):**
- Old: violations = [DUP-01, OB-01] (weight=2 → EXIT, not WARN)
- New: Added OB-02: [DUP-01, OB-01, OB-02] (weight=3 → WARN)
- Ensures test correctly validates band=WARN with recommendation=medium_risk_review

## Verification

- **All 79 behavioral tests pass** (test_risk_scoring_check.py)
- **All 75 adversarial tests pass** (test_risk_scoring_check_adversarial.py)
- **Total: 144 tests passing** (up from ~134 previously)
- **No implementation code changes required** (implementation was already correct)

## Summary

Weight-based band classification corrected across both test suites. All assertions now align with spec v1.1 mapping:
- Weight input domain [0-20] → Band classification (more direct, deterministic)
- Score output domain [0-100] → Informational only, not used for band assignment

Test vectors now correctly verify that implementation matches intended specification behavior.
