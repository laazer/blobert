# M11-01 Planning Run

**Run id:** `2026-05-23T-plan-run`  
**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/backlog/01_player_state_machine.md`  
**Stage:** PLANNING → SPECIFICATION  
**Planner:** Planner Agent

---

## Outcome

Planning complete. Execution plan embedded in ticket (10 tasks). M1 prerequisite satisfied (PlayerController3D + MovementSimulation exist). `scripts/player/player_state_machine.gd` absent — greenfield RefCounted FSM. Handoff to Spec Agent.

---

## Repo Discovery (Planning Evidence)

| Asset | Path | Relevance |
|-------|------|-----------|
| Player controller | `scripts/player/player_controller_3d.gd` | Uses `MovementSimulation.MovementState` for kinematics; no gameplay FSM |
| Kinematic sim | `scripts/movement/movement_simulation.gd` | `MovementState` inner class — **not** gameplay states |
| Reference FSM (enemy) | `scripts/enemy/enemy_state_machine.gd` | RefCounted, string states, guard no-ops |
| Reference FSM (scene) | `scripts/system/scene_state_machine.gd` | Headless test pattern in `tests/scripts/system/test_scene_state_machine.gd` |
| Death flow | `scripts/system/death_restart_coordinator.gd` | HP ≤ 0 → RunStateManager; no player DEAD enum today |
| Animation hints | `scripts/player/player_export_animation_controller_3d.gd` | idle/move/jump from floor + horizontal speed only |
| Downstream ticket | `backlog/02_physics_frame_order.md` | Depends on M11-01 state_timer; AC pre-checked — out of scope for this run |

**States in ticket AC not yet in code:** FLOAT, ABSORB, MUTATE, HURT, DEAD (gameplay layer). WALL_CLING exists via `is_wall_clinging`. IDLE/WALK/JUMP/FALL inferred from floor + velocity (animation controller pattern).

---

### [M11-01] PLANNING — MovementState vs gameplay FSM

**Would have asked:** Should `PlayerStateMachine` replace or wrap `MovementSimulation.MovementState`?

**Assumption made:** Keep both layers separate. `MovementSimulation.MovementState` remains the kinematic snapshot; new `PlayerStateMachine` is gameplay FSM owned by `PlayerController3D`. Spec must document naming (`PlayerState` enum vs `MovementState` class) to prevent conflation.

**Confidence:** High

---

### [M11-01] PLANNING — FLOAT / ABSORB / MUTATE without current mechanics

**Would have asked:** FLOAT has no hold-to-float input today; ABSORB/MUTATE are infection-loop concepts without explicit player states. Implement stubs or defer?

**Assumption made:** Spec Agent defines all 10 AC states with transition rules. Implementation must codify **observable current behavior** for movement states; ABSORB/MUTATE/HURT/DEAD/FLOAT get rules aligned with infection/death/damage flows (e.g., chunk stuck → ABSORB, `take_damage` → HURT, HP ≤ 0 → DEAD, FLOAT reserved/minimum-duration stub if no input yet). Non-breaking refactor: no new player-visible mechanics in this ticket.

**Confidence:** Medium

---

### [M11-01] PLANNING — Attack states out of scope

**Would have asked:** Include CHARGE_UP / ABILITY_USE in enum for forward compatibility?

**Assumption made:** Ticket Notes are binding — do **not** add attack-specific states. M11 core tickets add those later. Spec lists them only in "Future states" if needed for transition matrix clarity.

**Confidence:** High

---

### [M11-01] PLANNING — Test surface

**Would have asked:** Godot-only unit tests for FSM, or also integration tests through PlayerController3D?

**Assumption made:** Both. Pure RefCounted tests (pattern: `test_scene_state_machine.gd`) plus controller wiring tests proving regression suite unchanged. Full gate: `timeout 300 ci/scripts/run_tests.sh` exits 0.

**Confidence:** High

---

### [M11-01] PLANNING — Spec path

**Would have asked:** `agent_context/agents/2_spec/player_state_machine_spec.md` vs `project_board/specs/`?

**Assumption made:** Follow repo convention for gameplay specs under `agent_context/agents/2_spec/player_state_machine_spec.md` (matches `soft_death_and_restart_spec.md`, `enemy_state_machine` ticket reference). Spec exit gate type: `generic`.

**Confidence:** Medium
