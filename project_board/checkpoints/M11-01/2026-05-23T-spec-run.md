# M11-01 Spec Agent Run — 2026-05-23

**Run id:** `2026-05-23T-spec-run`  
**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/backlog/01_player_state_machine.md`  
**Stage:** SPECIFICATION → TEST_DESIGN

---

## Outcome

- Spec: `project_board/specs/player_state_machine_spec.md` (revision 1, type `generic`)
- Ticket stage: `TEST_DESIGN`, revision 3
- Handoff: `handoff-latest.yaml` (spec → test_designer)

---

## Frozen Decisions

| Topic | Decision |
|-------|----------|
| Spec path | `project_board/specs/player_state_machine_spec.md` (repo convention; planner path superseded) |
| Dual layer | Keep `MovementSimulation.MovementState` (kinematic) separate from `PlayerStateMachine.PlayerState` (gameplay) |
| Enum | 10 members: IDLE, WALK, JUMP, FALL, FLOAT, WALL_CLING, ABSORB, MUTATE, HURT, DEAD |
| Derivation priority | DEAD > HURT > ABSORB > MUTATE > WALL_CLING > air/ground kinematic |
| ABSORB vs MUTATE | ABSORB wins when chunk stuck |
| FLOAT | Not auto-derived in M11-01; explicit `transition(FLOAT)` only; MIN_FLOAT_FROM_JUMP_SEC = 0.05 |
| HURT duration | MIN_HURT_SEC = 0.0 (behavior-preserving; downstream may raise) |
| DEAD exit | `reset()` only |
| Attack states | Out of scope (CHARGE_UP, ABILITY_USE, etc.) |
| Frame order | Document hook only; M11-02 owns reorder |

---

### [M11-01] SPECIFICATION — Spec file location

**Would have asked:** Use `agent_context/agents/2_spec/player_state_machine_spec.md` or `project_board/specs/`?

**Assumption made:** Follow repo convention (`project_board/specs/` — 110+ gameplay specs). Handoff evidence points to `project_board/specs/player_state_machine_spec.md`.

**Confidence:** High

---

### [M11-01] SPECIFICATION — HURT observable duration

**Would have asked:** What minimum hurt duration preserves M1 behavior?

**Assumption made:** `MIN_HURT_SEC = 0.0`; HURT is latched one derivation cycle on damage. No new invulnerability or movement lock in M11-01.

**Confidence:** Medium

---

### [M11-01] SPECIFICATION — ABSORB vs MUTATE priority

**Would have asked:** When chunk is stuck and mutation slot is filled, which state wins?

**Assumption made:** ABSORB (priority 3) over MUTATE (priority 4) — chunk-on-enemy is the dominant infection signal.

**Confidence:** Medium

---

## Orchestrator Follow-ups

```bash
python ci/scripts/spec_completeness_check.py project_board/specs/player_state_machine_spec.md --type generic
python ci/scripts/run_workflow_transition_gates.py --ticket-id M11-01 --transition spec_to_test_design
```

## Spec Exit Gate

```
spec-completeness-check: player_state_machine_spec.md  type=generic
PASS: all required sections present.
```

## Workflow Transition Gates

```
transition=spec_to_test_design ticket_id=M11-01
  todo_validation_check: PASS — All todos completed for Spec Agent.
  handoff_validation_check: PASS — Handoff checklist valid for spec→test_designer.
```
