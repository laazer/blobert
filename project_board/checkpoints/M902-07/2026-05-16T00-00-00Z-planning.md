# Checkpoint Log: M902-07 Governance Audit Pipeline and Baseline

**Ticket:** `project_board/902_milestone_902_agent_predictabilitiy_improvements/01_active/07_governance_audit_pipeline_and_baseline.md`

**Run ID:** `2026-05-16T00-00-00Z-planning`

**Stage:** PLANNING

---

## Summary

Planner Agent read ticket M902-07 (Governance audit pipeline and `.governance-baseline.json`) and decomposed it into a structured execution plan. The ticket requires:

1. An audit command that scans the repo with the existing static analysis gate (M902-02 COMPLETE) and emits structured violation reports.
2. Clustering logic to group violations by rule + path prefix.
3. A baseline file (`.governance-baseline.json`) with expiration, ownership, rationale, and rule_id metadata.
4. Policy documentation for baseline updates and audit event metadata integration.

---

## Planning Decisions

### Design Question 1: Audit vs. Gate Orchestration

**Would have asked:** Should the audit command reuse the existing `static_analysis_check` gate from M902-02, or implement a separate audit pipeline?

**Assumption made:** The audit command will invoke the existing gate (via `ci/scripts/gate_runner.py`) and wrap its JSON output with clustering, baseline comparison, and reporting logic. This preserves the static analysis tooling investment and avoids duplication.

**Confidence:** High. The gate framework (M902-01) and static analysis tooling (M902-02) are stable and documented. Reuse is appropriate.

---

### Design Question 2: Baseline Granularity

**Would have asked:** Should the baseline track violations at (a) rule + path prefix level, (b) per-violation fingerprint (file + line + rule), or (c) per-file?

**Assumption made:** Baseline entries group by (rule, path_prefix), e.g., `ruff:F401:asset_generation/python/src/`. This reduces baseline file size and makes the policy easier to reason about. Fine-grained fingerprints are deferred to M903 if needed.

**Confidence:** Medium-High. The ticket mentions "clustering (by rule + path prefix)" which supports this decision.

---

### Design Question 3: Expiration Policy

**Would have asked:** What are sensible defaults for `expires_at`? Absolute dates? Relative (e.g., 90 days)? Configurable per entry or global?

**Assumption made:** Each baseline entry carries an absolute `expires_at` ISO 8601 date (e.g., "2026-08-15T00:00:00Z"). Entries without an expiration date are treated as permanent (no expiration field). A utility function will detect expired entries and emit warnings. The ticket mentions "expiration, ownership metadata" but does not specify defaults; M903 will add policy enforcement.

**Confidence:** Medium. This is reasonable but should be validated with the human if enforcement logic changes downstream assumptions.

---

### Design Question 4: Ownership Model

**Would have asked:** What does "owner" mean? GitHub username? Team name? Email? How is ownership validated or enforced?

**Assumption made:** The `owner` field is a free-text string (e.g., "alice@blobert.team" or "Backend Team"). No validation is enforced in this ticket; the field is logged and included in audit reports. Ownership enforcement/notification logic is deferred to M903.

**Confidence:** Medium. The ticket says "ownership metadata" but does not prescribe validation.

---

### Design Question 5: Baseline Schema and Validation

**Would have asked:** Should the baseline schema be validated against a JSON Schema during audit runs?

**Assumption made:** Yes. A `project_board/schemas/governance-baseline-schema.json` will define the structure. The audit command will validate the baseline file on load. Invalid baselines will cause the audit to fail with a clear error message. This prevents silent corruption.

**Confidence:** High. This is a basic hygiene requirement.

---

### Design Question 6: Remediation Ticket Generation

**Would have asked:** Should remediation tickets be auto-generated during audit, or should they be manually created based on a report?

**Assumption made:** The audit command will produce a markdown report suitable for human review. Remediation ticket generation (creation under `project_board/**/00_backlog/`) is deferred to M903 or a separate integration task. This ticket focuses on audit + baseline; ticket generation is a downstream integration.

**Confidence:** High. The ticket says "outputs markdown snippets suitable for `project_board/**/00_backlog/`" but does not require automation.

---

## Ambiguity Resolutions Logged

All critical assumptions have been checkpointed. No critical blockers identified. Plan proceeds to SPECIFICATION stage.

---

## Context Gathered

- **M902-01 (Validation Gate Framework):** COMPLETE. Gate runner at `ci/scripts/gate_runner.py`, gate registry at `ci/scripts/gate_registry.json`, schema fixtures at `ci/scripts/gate_schemas/`. 220 tests all PASS.
- **M902-02 (Static Analysis Gate Tooling):** COMPLETE. Gate `static_analysis_check` is implemented, tested, and registered in shadow mode. Runs ruff, mypy, bandit, vulture, import-linter, semgrep, wemake on Python; eslint on TypeScript; jscpd for duplication. JSON output matches gate schema.
- **Exclusions:** Per CLAUDE.md, generated artifacts (*.glb, *_export.png, *_bake.png), .venv/, node_modules/, reference_projects/, etc. are excluded from analysis.
- **Execution environment:** macOS (Darwin 25.3.0), Python 3.11+, uv available, Godot CLI available (based on project setup).

---

## Notes for Next Stages

- Spec Agent should read the destructive-api template? No—this ticket is about auditing and reporting, not destructive operations.
- Randomness/selection? No randomness in the core requirement.
- Umbrella ticket? No child tickets referenced; this is a standalone feature.
- Gating dependencies? M902-02 is complete and stable. No blocking issues.

Plan is ready for Spec Agent to execute.
