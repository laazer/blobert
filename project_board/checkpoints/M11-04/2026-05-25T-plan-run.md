# M11-04 Planning Run — 2026-05-25

**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/04_attack_resource.md`
**Agent:** Planner Agent
**Stage:** PLANNING → SPECIFICATION

---

## Discovery Summary

### Codebase state

- `scripts/attacks/` directory does **not exist**. This ticket creates the first file there.
- No existing Godot `Resource` subclass exists under `scripts/`. This is the project's first custom Resource class.
- Four existing enemy attack scripts (`adhesion_bug_lunge_attack.gd`, `acid_spitter_ranged_attack.gd`, `claw_crawler_attack.gd`, `carapace_husk_attack.gd`) all extend `Node` and use `@export` for tuning. They provide concrete parameter examples (damage, cooldown, range, knockback, etc.).
- Design spec: `project_board/specs/mutation_attack_system_design_spec.md` defines the canonical `AttackResource` schema.
- Test runner (`tests/run_tests.gd`) auto-discovers any `test_*.gd` file under `tests/`.
- Test utils base: `tests/utils/test_utils.gd` — provides `_assert_true`, `_assert_eq_float`, `_assert_eq_string`, etc.
- Downstream consumer: M11-05 (`attack_executor_handlers`) takes `AttackResource` as input to `execute_attack()`.

### Property list reconciliation (ticket vs. design spec)

| Property | Ticket | Design Spec | Decision |
|----------|--------|-------------|----------|
| `attack_id: int` | ✅ | ✅ | Include |
| `attack_name: String` | ✅ | ✅ | Include |
| `description: String` | ❌ | ✅ | **Spec Agent to decide** — low-cost addition, useful for HUD |
| `effect_type: String` | ✅ | ✅ | Include |
| `damage: float` | ✅ | ✅ | Include |
| `cooldown: float` | ✅ | ✅ | Include |
| `range: float` | ✅ (as `range`) | ✅ (as `range`) | Include — note: `range` is a GDScript built-in; Spec Agent must verify no shadowing |
| `startup_frames: int` | ✅ | ✅ | Include |
| `knockback_magnitude: float` | ✅ | ✅ | Include |
| `knockback_direction: String` | ✅ (ticket) | ❌ (spec uses modifier) | **Spec Agent to freeze** — ticket says top-level; spec puts it in modifiers |
| `projectile_speed: float` | ✅ | ✅ | Include |
| `projectile_lifetime: float` | ❌ | ✅ | **Spec Agent to decide** — design spec has it |
| `color: Color` | ✅ | ✅ | Include |
| `vfx_scale: float` | ✅ | ✅ | Include |
| `modifiers: Dictionary` | ✅ | ✅ | Include |

### Assumptions logged

### [M11-04] PLANNING — Property list reconciliation
**Would have asked:** The ticket specifies `knockback_direction` as a top-level export, but the design spec places it inside `modifiers`. Which is authoritative?
**Assumption made:** Defer to Spec Agent to freeze this — the ticket's acceptance criteria are authoritative for the ticket, but the Spec Agent should reconcile with the design spec and decide.
**Confidence:** Medium

### [M11-04] PLANNING — `range` identifier
**Would have asked:** GDScript has a built-in `range()` function. Will naming a property `range` on a Resource cause shadowing issues?
**Assumption made:** Spec Agent must test/decide. If shadowing is a problem, rename to `attack_range` (consistent with existing enemy scripts).
**Confidence:** Medium

### [M11-04] PLANNING — `description` and `projectile_lifetime` fields
**Would have asked:** Design spec includes `description: String` and `projectile_lifetime: float` but the ticket omits them. Should we add them?
**Assumption made:** Spec Agent to decide — conservative approach says include what the ticket AC says; design spec additions are optional enhancements.
**Confidence:** Medium

### [M11-04] PLANNING — Test scope for data-only Resource
**Would have asked:** What level of testing is appropriate for a data-only Resource class? Property access and serialization? Or also integration with downstream executor?
**Assumption made:** Tests cover: instantiation, default values, typed property access, serialization round-trip (duplicate/save/load), modifier dictionary operations, and example attack configurations from the ticket. No integration testing (that's M11-05).
**Confidence:** High

---

## Execution Plan

See below (also written to ticket).

---

## Outcome

PLANNING COMPLETE. Stage → SPECIFICATION. Handoff to Spec Agent.
