# M25-05 Bipedal body presets — autopilot run

### [M25-05] PLANNING — body_type scope
**Would have asked:** Should `player_slime` receive `body_type` in the API?
**Assumption made:** Only `ANIMATED_SLUGS` (six enemies), matching `_ANIMATED_ENEMY_SLUGS` and existing rig-rotation gating; `player_slime` unchanged.
**Confidence:** High

### [M25-05] IMPLEMENTATION — HumanoidSimpleRig tests
**Would have asked:** `_ImportHumanoidRig` has no `build_options`; OK to use `getattr`?
**Assumption made:** Use `getattr(self, "build_options", None)` and pass `dict` or `None` into `humanoid_leg_segment_count`.
**Confidence:** High

### [M25-05] AC — preview latency
**Would have asked:** AC says “immediately” while Web Editor notes say regeneration.
**Assumption made:** Follow ticket’s Web Editor Implementation: hint text documents regeneration; geometry changes apply on next Blender run.
**Confidence:** Medium
