### [M901-02] TEST_DESIGN — store public API naming
**Would have asked:** Exact function names for the store layer read/write surface (spec says “load, save” only).
**Assumption made:** Tests require `write_registry_json_atomic` and `read_registry_object` on `src.model_registry.store`, plus `registry_path`, matching deterministic JSON formatting (`indent=2`, `sort_keys=True`, trailing newline).
**Confidence:** Medium
