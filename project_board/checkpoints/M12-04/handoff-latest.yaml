handoff:
  schema_version: "1.0"
  ticket_id: M12-04
  from_agent: learning
  to_agent: ac_gatekeeper
  validated_at: "2026-05-29T00:00:00Z"
  required_items_met: 3
  total_required_items: 3
  checklist:
    - item_key: learning_insights_documented
      item: "Insights documented"
      status: complete
      evidence: project_board/LEARNINGS.md
    - item_key: learning_rationale_recorded
      item: "Decision rationale recorded"
      status: complete
      evidence: "Root causes: (1) GDScript coroutine called without await clears _is_active before hits land — fix: async wrapper with return; (2) _KNOWN_EFFECT_TYPES constant not updated when MELEE_SWIPE_COMBO added — fix: maintenance comment and count assertion; (3) _make_combo_resource/_make_db duplicated across test files — fix: attack_database_harness.gd static helpers; (4) pre-existing Python Ruff errors cause run_tests.sh exit 1 unrelated to M12-04 — AC Gatekeeper correctly classified as pre-existing; (5) subagents without Bash tool delegated all shell ops to orchestrator — document as relay step in workflow."
    - item_key: learning_checklist_validated
      item: "Handoff checklist validated"
      status: complete
      evidence: project_board/checkpoints/M12-04/todos-latest.json
