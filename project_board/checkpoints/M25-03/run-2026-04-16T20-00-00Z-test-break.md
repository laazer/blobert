# M25-03 Test Break Run — 2026-04-16T20-00-00Z

## Context

Test Breaker Agent extending `BuildControls.textureUpload.test.tsx` for ticket M25-03.
Spec: `project_board/specs/texture_upload_support_spec.md` (TUS-1..TUS-9).
Existing test file authored by Test Designer Agent covers 8+ base cases.

---

### [M25-03] TEST_BREAK — image/jpg MIME rejection: is it "rejected" or treated as image/jpeg?
**Would have asked:** The spec says accepted MIME types are `["image/png", "image/jpeg"]`. `"image/jpg"` is not in the whitelist. But should the test assert rejection or just note the browser behavior?
**Assumption made:** Per TUS-3 Risk Analysis: "'image/jpg' is not a valid MIME type (the correct type is 'image/jpeg'); if a browser emits 'image/jpg', it will be rejected. This is correct behavior per the whitelist." Test asserts `image/jpg` is rejected with the MIME error message. Conservative: any non-whitelisted MIME is rejected, full stop.
**Confidence:** High

### [M25-03] TEST_BREAK — Remove button type="button" attribute
**Would have asked:** The spec (TUS-7 Constraints) states `type="button"` must be set on the Remove button to prevent form submission. The designer's suite tests role/name but not the attribute directly. Should we test the attribute?
**Assumption made:** Yes. This is a spec-explicit constraint (TUS-7 Spec Summary: "The button must not appear... `type='button'` must be set"). The attribute test is adversarial: it catches an implementation that renders `<button>Remove</button>` (default type="submit") without the required attribute.
**Confidence:** High

### [M25-03] TEST_BREAK — reactive mode-switch custom→none→custom without remount
**Would have asked:** Does the React component restore file input correctly after mode transitions custom→none→custom without a full remount? This tests whether implementation uses conditional rendering or visibility toggling.
**Assumption made:** The spec says the file input "is not rendered under any other `texture_mode` value" (TUS-2), implying conditional rendering (mount/unmount), not visibility toggle. The test changes the store value and checks DOM presence reactively. If the component uses `display: none` instead of conditional rendering, the input would still be in the DOM when mode is "none" — this is the vulnerability this test targets.
**Confidence:** High

### [M25-03] TEST_BREAK — multiple files in FileList, only first processed
**Would have asked:** The spec says `e.target.files[0]` is validated. If `files` contains multiple entries, does the component only process the first one, or does it iterate?
**Assumption made:** Only the first file should be processed (spec says `e.target.files[0]` is the file to validate). Test verifies: with a valid first file and an invalid second file, only `createObjectURL` is called once with the first file.
**Confidence:** High

### [M25-03] TEST_BREAK — upload while in error state: previous error clears on valid re-upload
**Would have asked:** Should the store state (customTextureUrl) be checked after clearing an error via valid re-upload, not just the DOM?
**Assumption made:** Yes — DOM assertions alone can produce false confidence if the component renders error=null but fails to update the store. Test checks both: error span absent from DOM AND store's `customTextureUrl` is the new blob URL.
**Confidence:** High

### [M25-03] TEST_BREAK — URL.createObjectURL throwing
**Would have asked:** If `URL.createObjectURL` throws (e.g. security policy), should the component crash or handle gracefully?
**Assumption made:** The spec does not explicitly mandate try/catch around `URL.createObjectURL`. A crash (unhandled exception) would break the entire editor. Conservative assumption: implementation should not crash. The test verifies the component remains mounted and no unhandled error propagates. Mark as CHECKPOINT — if implementation does NOT wrap in try/catch, this test will expose the gap.
**Confidence:** Medium

### [M25-03] TEST_BREAK — empty string file name
**Would have asked:** Should a file with empty string name be treated differently (e.g. bypass MIME validation)?
**Assumption made:** File name is not part of the validation logic per spec (only `file.type` and `file.size` are checked). An empty name with invalid MIME should still be rejected. The test verifies this.
**Confidence:** High

### [M25-03] TEST_BREAK — zero-byte file with valid MIME
**Would have asked:** Is a zero-byte file accepted? The spec says `file.size <= 2 * 1024 * 1024` with no lower bound.
**Assumption made:** A zero-byte file satisfies `0 <= 2097152` so it should be accepted. The test verifies `createObjectURL` is called for a 0-byte PNG. This is a boundary test: implementation might accidentally use `file.size > 0` as an additional guard not in the spec.
**Confidence:** High

### [M25-03] TEST_BREAK — spy reset between tests
**Would have asked:** The base test file creates spies in `beforeEach` and restores in `afterEach`. The adversarial file must ensure its own spy lifecycle doesn't pollute.
**Assumption made:** Adversarial file uses its own `beforeEach`/`afterEach` blocks scoped to the file; the URL stub installation is idempotent (checks before `defineProperty`). This matches the pattern from the base test file.
**Confidence:** High

### [M25-03] TEST_BREAK — exactly 2097152 bytes via store state assertion (not just DOM)
**Would have asked:** The base test checks `createObjectURLSpy.toHaveBeenCalledOnce()` but not the store state after exact-boundary upload. Should we verify store state too?
**Assumption made:** Yes. Store state assertion (`customTextureUrl === "blob:test-url"`) is stronger than call-count alone. If implementation calls `createObjectURL` but fails to call `setCustomTextureUrl`, the call-count assertion passes but the store is wrong. Adversarial test catches this.
**Confidence:** High
