# Checkpoint Log: M25-03 — SPECIFICATION run

**Ticket:** `project_board/25_milestone_25_enemy_editor_visual_expression/in_progress/03_texture_upload_support.md`
**Run ID:** `2026-04-16T15-00-00Z-spec`
**Stage:** SPECIFICATION

---

### [M25-03] SPECIFICATION — Blob URL storage: store vs local state

**Would have asked:** The ticket says "store it in local component state (not in Zustand)" for the object URL, but also says "pass the URL to GlbViewer as a prop or via a dedicated store slice" — and then the Execution Plan for Task 2 adds `customTextureUrl: string | null` to the Zustand store. Which is the authoritative approach?

**Assumption made:** The Execution Plan (Task 2, Task 3) is the authoritative resolution: the blob URL lives in the Zustand store (`customTextureUrl`), not in local component state. The ticket prose "store it in local component state" was an earlier draft and the Execution Plan supersedes it. The local state in BuildControls is limited to `uploadError: string | null` only.

**Confidence:** High (Execution Plan is explicit and more recent)

---

### [M25-03] SPECIFICATION — `useEffect` cleanup for blob URL: unmount vs URL change

**Would have asked:** The Execution Plan (Task 3, step 7) says to add a cleanup `useEffect` with `[customTextureUrl]` in the dep array, then notes "this fires on unmount or URL change." A `useEffect` cleanup with `[customTextureUrl]` will fire on each URL change too — that would revoke the previous URL, which is correct. But the body of the effect does nothing on mount (no side effect to set up). Is this an intentional cleanup-only pattern?

**Assumption made:** Yes, this is an intentional cleanup-only pattern. The effect has no setup body; its only purpose is the cleanup function that revokes the previous blob URL when `customTextureUrl` changes (before the next URL is stored) or on unmount. This pattern is valid React cleanup and matches what the ticket specifies.

**Confidence:** High

---

### [M25-03] SPECIFICATION — Python comment exact location (before vs after `_TEXTURE_MODE_OPTIONS`)

**Would have asked:** Task 1 says "immediately after `_TEXTURE_MODE_OPTIONS` (or inside the function body)." The constant is at module level; the comment on the `"custom"` exclusion could go either as a trailing comment on the constant line or as a docstring addition inside `_texture_control_defs()`.

**Assumption made:** The comment goes inside `_texture_control_defs()` as a standalone comment line immediately after the function docstring (before the `return` statement), to keep it co-located with the logic it explains. The constant itself is not altered.

**Confidence:** Medium — either location is defensible; chosen for proximity to the function that emits the options list.

---

### [M25-03] SPECIFICATION — Spec completeness check type

**Would have asked:** The workflow enforcement requires running `python ci/scripts/spec_completeness_check.py <spec_path> --type <ticket_type>`. This ticket is a frontend/Python comment feature with no destructive, API mutation, randomness, or load-open aspects.

**Assumption made:** `--type generic` is correct. The spec completeness check will be a no-op gate (generic skips check body).

**Confidence:** High
