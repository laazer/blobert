### [M5-LJEC] PLANNING — Limb Joints and End Caps execution plan

**Would have asked:** The ticket's `_mesh_str` spec says `BaseAnimatedModel._mesh_str(name) -> str` reads `build_options["mesh"]` and validates against allowed tokens. However, `BaseAnimatedModel._mesh` currently handles int/float overrides only and does NOT handle string values at all (it returns `float(raw)` for non-int, non-tuple bases). Should `_mesh_str` be a new separate method or should `_mesh` be extended?
**Assumption made:** Implement `_mesh_str` as a **new separate method** on `BaseAnimatedModel`, keeping `_mesh` unchanged for numeric overrides. `_mesh_str` will validate against a class-level `ClassVar[tuple[str, ...]]` or a passed-in allowed set. Consistent with how `_mesh` works for numeric types.
**Confidence:** High

---

**Would have asked:** The ticket says `ARM_END_SHAPE` / `LEG_END_SHAPE` with values `none`, `sphere`, `box`. Should these be defined on `HumanoidSimpleRig` as `ClassVar[str]` class variables, or on individual enemy classes like `AnimatedImp`?
**Assumption made:** Define `ARM_END_SHAPE` and `LEG_END_SHAPE` as `ClassVar[str]` tuners on `HumanoidSimpleRig` (default `"none"`). Each concrete enemy class can override for its specific defaults (e.g. `AnimatedCarapaceHusk` uses `ARM_END_SHAPE = "none"` explicitly per the ticket). This matches the ClassVar pattern already used for all other tuners on `HumanoidSimpleRig`.
**Confidence:** High

---

**Would have asked:** The `LIMB_JOINT_BALL_SCALE` and `LIMB_JOINT_VISUAL` tuners — where do they live? On `HumanoidSimpleRig`, or on the `limb_mesh` helper, or on each concrete enemy?
**Assumption made:** Define `LIMB_JOINT_BALL_SCALE: ClassVar[float]` and `LIMB_JOINT_VISUAL: ClassVar[bool]` on `HumanoidSimpleRig` with the ticket-specified defaults (scale ratio and True respectively). The `limb_mesh` helper accepts these as parameters rather than being a ClassVar consumer itself (keeps it pure/testable).
**Confidence:** Medium

---

**Would have asked:** The ticket says `ARM_SEGMENTS` / `LEG_SEGMENTS` surface in frontend `BuildControls` automatically via "ClassVar[int] discovery." Is there existing introspection in the frontend that already discovers ClassVar[int] fields? If not, does the plan need a frontend task?
**Assumption made:** Per the ticket's Acceptance Criteria: "No additional wiring needed — ClassVar[int] discovery is sufficient." The frontend already discovers ClassVar[int] fields. No frontend task is needed in this plan. Planner marks this as an assumption with no frontend task.
**Confidence:** Medium

---

**Would have asked:** Should `limb_chain.py` use `mathutils.Vector` directly (requiring Blender runtime), or should it be pure Python with plain tuples/named tuples, making it testable without Blender?
**Assumption made:** `limb_chain.py` will use `mathutils.Vector` since `BoneSpec` already uses `Vector` in `rig_types.py`. The existing test suite mocks `mathutils` (see `tests/conftest.py`). Therefore `limb_chain.py` is pure logic with Vector arithmetic; it is testable in the existing pytest harness.
**Confidence:** High

---

**Would have asked:** For `AnimatedImp.apply_themed_materials`, the current code hardcodes arm+hand pairs then leg cylinders as `lp=LIMB_PAIRS` iterations. When `ARM_SEGMENTS=2`, each arm produces 2 cylinders + 1 joint sphere + 1 hand = 4 parts. The existing `part_index` counter logic will break. How should the indexing be updated?
**Assumption made:** After the mesh helper migration, `build_mesh_parts` will call the mesh helper which returns a sub-list of parts for each limb. `AnimatedImp.apply_themed_materials` will be updated to apply materials by counting actual added parts per limb group (tracked by how many parts the helper added). The simplest robust approach: track part counts per group during build, or apply a sentinel approach. Conservative assumption: store `_arm_part_count` and `_leg_part_count` on `self` during `build_mesh_parts`, and read them in `apply_themed_materials`.
**Confidence:** Medium
