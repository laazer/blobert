# Checkpoint log — death_animation_playthrough — Spec Agent — run-2026-04-04-spec

## Entry 1 — Scene path naming

**Would have asked:** Should the 12 generated paths use `_animated_00` suffixes as in `test_enemy_scene_animation_wiring.gd`, or literal `_00` as in the ticket prose?

**Assumption made:** Canonical paths are `res://scenes/enemies/generated/{family}_animated_{00|01|02}.tscn` per the existing animation wiring test suite and WAGS spec family list.

**Confidence:** High (verified in repo).

## Entry 2 — Animation completion signal

**Would have asked:** Exact Godot API for “Death finished” (signal name vs polling `is_playing()`).

**Assumption made:** Spec states completion via `AnimationPlayer` end-of-play semantics (e.g. `animation_finished` for non-looping `Death`) without mandating a single API name so implementer can choose the correct Godot 4.6 API.

**Confidence:** Medium (implementation must bind to actual engine signal; tests will lock behavior).

## Entry 3 — Enemy root for queue_free

**Would have asked:** Is `queue_free()` on the `EnemyInfection3D` root required, or may a parent wrapper be freed instead?

**Assumption made:** The node that owns the ESM and scene contract for the 12 generated enemies (`EnemyInfection3D` root) is freed; if the scene tree nests it, freeing that root is sufficient for “enemy removed cleanly.”

**Confidence:** High for generated scenes; aligns with ticket “node is freed.”
