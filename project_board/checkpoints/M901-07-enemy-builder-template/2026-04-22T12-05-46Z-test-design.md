### [M901-07-enemy-builder-template] TEST_DESIGN — template execution seam
**Would have asked:** Should template phase orchestration be asserted via source inspection or strictly through executable hook-order behavior?
**Assumption made:** Enforce executable behavior only. Primary tests use a probe subclass and runtime event capture to validate deterministic order (`body -> limbs -> materials -> zone extras`) and single-pass phase execution.
**Confidence:** High

### [M901-07-enemy-builder-template] TEST_DESIGN — local runner availability
**Would have asked:** Which Python test environment should run primary pytest evidence when the default interpreter lacks `pytest`?
**Assumption made:** Author runtime contracts and record local execution blocker (`python3 -m pytest ...` fails with `No module named pytest`), deferring execution evidence to downstream environment/agent with project test tooling.
**Confidence:** High
