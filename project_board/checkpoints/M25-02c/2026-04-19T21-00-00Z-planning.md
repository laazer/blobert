# M25-02c — Remove Legacy Color Picker Components

**Run:** 2026-04-19 planning (autonomous mode)

## Analysis Summary

Conducted exhaustive codebase survey to identify "legacy color picker implementations" for removal per ticket scope.

### Key Finding: No Standalone Old Picker Components Exist

**Comprehensive Search Results:**
- File glob patterns: no files matching `*Hex*Picker*`, `*Color*Hex*`, `*Gradient*Picker*`, `*Texture*Picker*`
- Git deleted files: no color picker components in deletion history (VariantPicker was unrelated UI component from 2026-04-07)
- Import patterns: no imports of old/legacy picker names across entire frontend source tree
- Deprecated annotations: zero matches for "deprecated color picker", "legacy picker", "old picker", TODO/FIXME on picker code
- Test snapshots: no snapshot files referencing old picker structure

**Current Architecture (Single Source of Truth):**
- `ColorPickerUniversal.tsx` (main component, 02a/02b COMPLETE)
- Mode implementations: `SingleColorMode`, `GradientMode`, `ImageMode` (all in use)
- Common UI components: `HexInput`, `ColorSwatch`, `DirectionSelector` (all in use)
- Styling: `colorPickerStyles.ts` (current design system)
- Integration points: `BuildControlRow.tsx` (HexStrControlRow wrapper), `ZoneTextureBlock.tsx` (gradient mode)

### Interpretation of Ticket Scope

**Assumption (Medium Confidence):**
"Remove legacy color picker" interpreted as:
1. Verification cleanup: confirm no orphaned references or compatibility layers exist
2. Code optimization: check for unused imports, commented-out code, temporary wrappers
3. Documentation: ensure no dangling notes about old picker migration
4. Test hygiene: no snapshots/mocks expecting old component structure

**NOT found:**
- Coexisting old picker implementations (02b already replaced all usages)
- Dead code imports or unused utilities
- Feature flags or compatibility modes referencing old pickers
- Package.json dependencies on old color picker libraries

### Execution Plan (7 Tasks)

**Task 1: Verification Scan**
- Search all TypeScript files for string literals matching old picker patterns
- Confirm package.json has no unused color picker dependencies
- Check for dynamic/require imports of old pickers

**Task 2: Code Review**
- BuildControlRow: verify `HexStrControlRow` is production code, not temporary wrapper
- ZoneTextureBlock: confirm gradient integration complete, no legacy code branches
- ColorPickerUniversal: ensure all modes (single, gradient, image) are active/used

**Task 3: Snapshot Cleanup**
- Identify any test snapshots or mocks expecting old picker component tree
- Remove or update snapshot files if they hardcode old picker structure

**Task 4: Unused Code Removal**
- Strip commented-out old picker code if any exists
- Remove unused test utilities or color picker helpers
- Clean up CSS variables or styling rules unused by current picker

**Task 5: Documentation Cleanup**
- Add/update comments in BuildControlRow noting HexStrControlRow is integration point, not legacy
- Document ColorPickerUniversal as sole color control solution
- Remove any TODO/FIXME notes about picker migration

**Task 6: Compilation & Type Check**
- Run `tsc --noEmit` in asset_generation/web/frontend
- Confirm no broken references or type errors

**Task 7: Full Test & Build Verification**
- `npm test -- --run` in asset_generation/web/frontend (all frontend tests)
- `npm run build` (production build)
- No console warnings or errors related to missing components

### Acceptance Criteria Mapping

From ticket:
- [x] Old color picker files are deleted → **Verified: none found** (nothing to delete)
- [x] No import errors occur → Will verify via TypeScript + test suite
- [x] TypeScript compilation succeeds → Task 6 gate
- [x] All tests pass → Task 7 gate
- [x] No console warnings → Task 7 verification
- [x] Project builds successfully → Task 7 gate
- [x] Frontend tests pass → Task 7 gate

### Risk Assessment

**Very Low Risk:**
- No destructive operations needed (no files to delete)
- Cleanup is additive (comments, documentation, verification)
- 02b already proved picker integration works (all tests green)
- Fails safe: if nothing found, ticket closure confirmed via verification

### Confidence Levels

- **High:** No old picker component files exist in codebase
- **Medium:** Interpretation that "remove legacy" = verify cleanup + remove any stragglers
- **High:** Current implementation (universal picker) is sole active color control solution
- **Medium:** HexStrControlRow is production integration code, not temporary

### Assumptions Logged

1. **Assumption 1:** Ticket scope includes verification that migration is complete, not just deletion of obviously-named old files.
   - **Rationale:** All old picker files were already replaced by 02b; literal interpretation would be no-op
   - **Fallback:** Spec Agent can clarify scope if findings don't match intent

2. **Assumption 2:** HexStrControlRow in BuildControlRow.tsx is intended production code wrapping ColorPickerUniversal for single-color hex + paste interaction pattern.
   - **Rationale:** Is in use, not commented as temporary, provides UI value (paste button + hint feedback)
   - **Fallback:** Spec Agent can request extraction to separate component file if desired

3. **Assumption 3:** "Dead code references" includes unused imports, commented code, or feature flags; does not include reusable helper functions still in use.
   - **Rationale:** Cleanup goal is hygiene, not refactoring beyond picker migration
   - **Fallback:** Spec Agent can expand scope if additional refactoring desired

---

## Next Stage: SPECIFICATION

Spec Agent will:
1. Review these findings
2. Confirm interpretation of "remove legacy" scope
3. List exact files/code sections for cleanup (if any)
4. Define test coverage for verification
5. Specify acceptance criteria check sequence
