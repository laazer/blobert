# attack_telegraph_system — Specification (2026-04-06)

## Deliverables

- Formal specification: `project_board/specs/attack_telegraph_system_spec.md` (ATS-1 … ATS-9, ATS-NF1, ATS-NF2).
- Ticket updated: Stage → TEST_DESIGN, Revision incremented, routed to Test Designer Agent.

---

### [attack_telegraph_system] SPECIFICATION — Unified export naming

**Would have asked:** Should `telegraph_fallback_seconds` be renamed to a single canonical `telegraph_duration_seconds` on all attack scripts, or keep legacy names for acid/adhesion?

**Assumption made:** Spec **ATS-5** allows existing export names if behavior matches (minimum floor 0.3 s, inspector-tunable); optional rename is an implementation choice if tests/scenes migrate consistently.

**Confidence:** Medium

---

### [attack_telegraph_system] SPECIFICATION — Carapace/claw attack behavior

**Would have asked:** What is the intended active-phase attack for carapace_husk and claw_crawler (melee vs ranged)?

**Assumption made:** **ATS-8** requires minimal attack entry points satisfying telegraph + no-damage-during-telegraph; full combat design can align with future tickets. Active phase may be placeholder until `hitbox_and_damage_system` lands, provided tests prove the telegraph contract.

**Confidence:** Medium

---

### [attack_telegraph_system] SPECIFICATION — 0.3 s vs Attack animation length

**Would have asked:** If the Attack clip is longer than 0.3 s, does active phase start at clip end or at 0.3 s?

**Assumption made:** **ATS-2** + **ATS-7**: Floor is **0.3 s minimum**; if animation drives telegraph, active phase begins when telegraph **completes** per controller (clip end), which is ≥ 0.3 s. If clip would finish **under** 0.3 s, hold until 0.3 s wall-clock before active phase.

**Confidence:** High
