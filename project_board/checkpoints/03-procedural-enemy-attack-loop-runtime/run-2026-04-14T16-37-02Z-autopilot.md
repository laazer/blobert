# Scoped Checkpoint Log

Run: 2026-04-14T16-37-02Z-autopilot
Ticket: project_board/10_milestone_10_procedural_enemies_in_level/in_progress/03_procedural_enemy_attack_loop_runtime.md
Stage: PLANNING

## Notes

- Autopilot single-ticket run initialized.
- Ticket dequeued from backlog to in_progress; WORKFLOW STATE normalized.

### [03_procedural_enemy_attack_loop_runtime] PLANNING — ESM stub vs M8 state machine
**Would have asked:** Must generated enemies use a real `EnemyStateMachine` (or behavioral equivalent) for clip selection and attack gating, or is the existing `GeneratedEnemyEsmStub` returning `idle` sufficient if family attack scripts and `EnemyAnimationController` still drive telegraph/hitbox cycles?
**Assumption made:** Spec Agent will trace `EnemyBase.get_esm()`, `enemy_generated_esm_stub.gd`, and `enemy_animation_controller.gd` against M10-01 R4/R5; the plan treats “no duplicate damage pipelines” and “continuous attack loop” as the hard requirements, with ESM fidelity as an explicit spec decision (minimal stub extension vs wiring real ESM) rather than guessing in PLANNING.
**Confidence:** Medium

### [03_procedural_enemy_attack_loop_runtime] PLANNING — `.attacks.json` timing authority
**Would have asked:** For this milestone, is companion `.attacks.json` mandatory for cooldown/hit-window timing on procedural instances, or is clip-driven / script-export timing authoritative?
**Assumption made:** Spec will either bind timing to existing family `*_attack.gd` exports and `EnemyAnimationController` behavior (aligned with current M8 scripts) or define a narrow loader contract if JSON is in scope; planner defers to spec with default bias toward documented clip/script authority and optional JSON to avoid pipeline churn.
**Confidence:** Medium
