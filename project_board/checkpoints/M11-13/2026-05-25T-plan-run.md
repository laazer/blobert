# Checkpoint Log: M11-13 Planning Run

**Ticket:** `project_board/11_milestone_11_base_mutation_attacks/in_progress/13_verify_damage_knockback.md`
**Stage:** PLANNING
**Run ID:** 2026-05-25T-plan-run
**Agent:** Planner Agent

---

### [M11-13] PLANNING — Scope classification

**Would have asked:** Is this ticket purely a test-addition ticket, or should the spec also cover adding `take_damage()` / `apply_poison()` / `apply_acid()` / `apply_slowness()` to `EnemyBase`?

**Assumption made:** The ticket is a verification ticket. The existing implementation (AttackResource, AttackExecutor, PlayerProjectile3D, AttackDatabase, PlayerController3D._try_attack()) is complete and functional. `EnemyBase` integration methods are explicitly out-of-scope per the AEX spec (Section 6: Deferred Boundary). This ticket adds cross-mutation integration tests and verifies end-to-end correctness using mock enemies, not real EnemyBase methods. No implementation changes are expected unless tests reveal bugs.

**Confidence:** High

---

### [M11-13] PLANNING — Test coverage gap analysis

**Would have asked:** Which specific test scenarios are missing from the existing 80+ tests?

**Assumption made:** After reviewing all 9 existing test files covering AttackExecutor, AttackResource, AttackDatabase, and their adversarial suites, the following gaps exist relative to the ticket AC:

1. **Cross-mutation integration tests** — No test wires AttackDatabase → lookup → AttackExecutor.execute_attack() for specific mutation configurations (Claw/MELEE_SWIPE, Acid/PROJECTILE_SPIT, Carapace/MELEE_SWIPE+away, Adhesion/MELEE_SWIPE+slow). Existing tests use generic resources.
2. **VFX position correctness** — ADV-21 verifies color/scale but not that the VFX position equals `owner_pos + facing * range * HITBOX_RANGE_FACTOR`.
3. **PlayerProjectile3D on-hit behavior** — Projectile creation is tested, but `_on_body_entered` → damage + knockback + modifier application is not integration-tested.
4. **Knockback direction correctness in end-to-end melee** — Unit tests cover `_calculate_knockback()` in isolation, but no test verifies that a full melee execution with "toward" direction actually produces a pull-toward-player knockback on the MockEnemy.
5. **Projectile velocity correctness** — No test verifies `_physics_process` moves the projectile at `direction_x * speed * delta`.

**Confidence:** High

---

### [M11-13] PLANNING — Spec type classification

**Would have asked:** What spec exit type should this verification ticket use for the completeness check?

**Assumption made:** This is a `generic` type ticket (not destructive, not api, not randomness, not load-open). The spec completeness check will use `--type generic`.

**Confidence:** High

---

## Estimated Effort

| Task | Agent | Estimated Effort |
|------|-------|-----------------|
| 1. Specification | Spec Agent | ~30 min |
| 2. Test Design | Test Designer Agent | ~45 min |
| 3. Test Breaking | Test Breaker Agent | ~30 min |
| 4. Implementation (if needed) | Implementation Agent | ~15 min (likely no-op or minor fixes) |
| **Total** | | **~2 hours** |
