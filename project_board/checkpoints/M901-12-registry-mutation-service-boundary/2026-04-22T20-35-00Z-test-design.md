### [M901-12] TEST_DESIGN — service exception taxonomy for transport mapping
**Would have asked:** Should service-layer conflict rejections use a dedicated domain error type or a built-in exception class?
**Assumption made:** Use `RuntimeError` as the strictest conservative conflict signal in test contracts so router mapping can assert `409` without binding to helper internals.
**Confidence:** Medium
