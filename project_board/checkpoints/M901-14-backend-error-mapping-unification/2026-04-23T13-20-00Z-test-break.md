### [M901-14] TEST_BREAK — Router scope ambiguity
**Would have asked:** Should adversarial error-mapping tests enforce unknown-exception safety on `/api/run/*` and `/api/files/*` now, even though current router behavior may still be pre-unification and leak/raw-map start failures?
**Assumption made:** Yes. Add strict behavior tests that encode a conservative, fail-closed shared fallback contract across in-scope router families (`registry`, `assets`, `run`, `files`) and allow them to fail RED until implementation unifies mapping.
**Confidence:** Medium
