# Checkpoint Log: M25-MTE Spec Run
**Ticket:** `project_board/25_milestone_25_enemy_editor_visual_expression/in_progress/06_mouth_and_tail_extras.md`
**Stage:** SPECIFICATION
**Run ID:** 2026-04-15T01-00-00Z-spec

---

### [M25-MTE] SPECIFICATION — imp and carapace_husk geometry-wired scope confirmation

**Would have asked:** The planning checkpoint (run-2026-04-15T00-00-00Z-planning.md) logged at Medium confidence that imp and carapace_husk should be geometry-wired for mouth/tail because they have explicit head spheres and body meshes. The ticket description says "controls declared but silently skipped on slugs without the relevant mesh object" — imp and carapace_husk DO have the relevant mesh objects. Should the spec confirm all 6 animated slugs as fully geometry-wired?

**Assumption made:** All 6 animated slugs (spider, slug, imp, spitter, claw_crawler, carapace_husk) are geometry-wired for both mouth and tail. The Planner's codebase findings section explicitly states "ALL TAIL-WIRED" for all six animated slugs and "MOUTH-WIRED" for each of the six individually (including imp and carapace_husk). This is consistent with the task breakdown table in the ticket. The spec reflects this: full geometry wiring for all 6.

**Confidence:** High — Planner explicitly confirmed each slug individually in the Codebase Findings section.

---

### [M25-MTE] SPECIFICATION — `tail_length` float block placement

**Would have asked:** The control assembly ordering requires `tail_length` to appear in the float section before mesh floats, but `_tail_control_defs()` returns a mix of non-float (`tail_enabled`, `tail_shape`) and float (`tail_length`) controls. How should the existing non-float/float split handle this?

**Assumption made:** The implementation must split `_tail_control_defs()` output through the same `static_non_float / static_float` filter that already handles the `static` list. `tail_enabled` and `tail_shape` (type `bool` and `select_str`) go into the non-float block; `tail_length` (type `float`) goes into the float section between the existing float static controls and `_mesh_float_control_defs(slug)`. The spec's control assembly ordering table (steps 7–10) makes this explicit.

**Confidence:** High — confirmed by reading `animated_build_controls_for_api()` implementation which already performs this split.

---

### [M25-MTE] SPECIFICATION — `create_mouth_mesh` `head_scale` argument derivation

**Would have asked:** The ticket specifies `create_mouth_mesh(shape, location, head_scale)`. What should `head_scale` be derived from in the caller? `_zone_geom_head_radii.x`, the average of radii, or some other scalar?

**Assumption made:** `head_scale = head_radii.x` — the X (forward-facing) half-extent of the head ellipsoid. This is the dominant dimension in the mouth placement direction and provides a consistent size reference for mouth primitives relative to head size. This is consistent with `eye_scale = eye_radius` pattern in the eye system.

**Confidence:** Medium — no explicit statement in the ticket; derived from the placement formula and existing eye system pattern.

---

### [M25-MTE] SPECIFICATION — Scale ratio constant numeric values

**Would have asked:** The exact numeric values for mouth and tail scale ratio constants (e.g., `_MOUTH_SMILE_X_RATIO`) are aesthetic design choices. Should the spec mandate specific values?

**Assumption made:** The spec does not mandate specific numeric values for scale ratios. It mandates only: (a) all ratios are named constants (not inline literals), and (b) certain relational constraints must hold (e.g., `_MOUTH_FANG_X_RATIO < _MOUTH_BEAK_X_RATIO`, `_TAIL_WHIP_XY_RATIO < _TAIL_SEG_XY_RATIO`, `_TAIL_CURLED_Z_RATIO < _TAIL_CURLED_X_RATIO`). The implementation agent has creative latitude on specific values, consistent with how eye/pupil ratio values were chosen in ESPS.

**Confidence:** High — this matches the precedent set in ESPS-3 where only the existence of named constants was mandated.
