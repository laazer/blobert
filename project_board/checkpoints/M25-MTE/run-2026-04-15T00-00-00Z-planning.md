# Checkpoint Log: M25-MTE Planning Run
**Ticket:** `project_board/25_milestone_25_enemy_editor_visual_expression/in_progress/06_mouth_and_tail_extras.md`
**Stage:** PLANNING
**Run ID:** 2026-04-15T00-00-00Z-planning

---

### [M25-MTE] PLANNING — Slug head/body wiring scope

**Would have asked:** The ticket assumes spider, slug, imp, claw_crawler, carapace_husk have explicit head mesh objects, and spitter may be deferred. Which slugs should receive tail geometry wiring?

**Assumption made:** All six animated slugs (spider, slug, imp, spitter, claw_crawler, carapace_husk) create a distinct `head` sphere in `build_mesh_parts`, giving the full set (`_zone_geom_head_center`/`_zone_geom_head_radii` set). Therefore ALL six slugs qualify for mouth wiring on the head zone. Spitter's head is front-facing (hx-forward sphere) just like slug/claw_crawler — nothing structurally prevents mouth placement; spitter is included in mouth-wired slugs. For tail: body sphere is created by every animated slug, so all six slugs qualify for tail wiring. `player_slime` is not an animated enemy class (absent from `AnimatedEnemyBuilder.ENEMY_CLASSES`) but appears in the controls API; it should be controls-only for both mouth and tail (no geometry).
**Confidence:** High — confirmed by reading all six enemy `build_mesh_parts` implementations.

---

### [M25-MTE] PLANNING — Tail placement axis

**Would have asked:** The ticket specifies tail exits from -X axis in Blender world space. How should the tail surface point be computed given body ellipsoid radii vary per slug?

**Assumption made:** Per the existing pattern in `zone_geometry_extras_attach.py` and `_zone_geom_body_center`/`_zone_geom_body_radii`, the spec should define tail root point as: `body_center + Vector((-body_radii.x, 0, 0))` (i.e., the -X surface of the body ellipsoid, analogous to front-face placement). This computation matches how zone extras already locate the back face (`place_back=True` → -X direction). The spec agent should document this formula.
**Confidence:** High

---

### [M25-MTE] PLANNING — `tail_length` float control placement in ordering

**Would have asked:** The eye/pupil system enforces that non-float controls appear before mesh float controls (ESPS-1-AC-6). Should mouth/tail controls follow the same ordering rule (all 4 non-float toggles/selectors before any float, with `tail_length` as the one float)?

**Assumption made:** Yes. The `animated_build_controls_for_api` assembly already splits `static` defs into `static_non_float` and `static_float`. The new `_mouth_control_defs()` and `_tail_control_defs()` should be merged the same way: boolean and select_str controls (mouth_enabled, mouth_shape, tail_enabled, tail_shape) before float controls, with `tail_length` landing in the float section. The spec should enumerate the insertion position explicitly.
**Confidence:** High — confirmed by reading `animated_build_controls_for_api()` assembly logic.

---

### [M25-MTE] PLANNING — coerce_validate path for new keys

**Would have asked:** Do the new 6 keys (`mouth_enabled`, `mouth_shape`, `tail_enabled`, `tail_shape`, `tail_length`) need to be added to `_coerce_and_validate` via `static_defs.extend(...)` or via a new helper, and does `options_for_enemy` need changes to `allowed_non_mesh`?

**Assumption made:** The validate module (`animated_build_options_validate.py`) already iterates `static_defs` which are built from `_eye_shape_pupil_control_defs()` + slug-specific controls. Adding `_mouth_control_defs()` and `_tail_control_defs()` to that list (exactly as `_eye_shape_pupil_control_defs()` is) will make coercion automatic. `options_for_enemy` must add the 6 new keys to `allowed_non_mesh` so they pass through the merge. This mirrors the exact pattern used for eye/pupil.
**Confidence:** High

---

### [M25-MTE] PLANNING — `imp` and `carapace_husk` eye mesh

**Would have asked:** `animated_imp.py` and `animated_carapace_husk.py` do not import `create_eye_mesh`/`create_pupil_mesh` and their `build_mesh_parts` do not call them — they are "controls-only" for eye/pupil. Should they be "controls-only" for mouth/tail too, or do they get mouth/tail geometry?

**Assumption made:** Imp and carapace_husk both create an explicit head sphere in `build_mesh_parts`. Therefore they SHOULD receive mouth geometry on the head zone (same as spider/slug/claw_crawler). They both also create an explicit body cylinder in `build_mesh_parts`, so they should receive tail geometry on the body zone. These are geometry-wired slugs for the mouth/tail system, unlike the eye system where they were controls-only (imp/carapace_husk lack stalk eyes). The ticket's "blob-only heads" framing does not apply here — both have distinct head mesh objects. This is a refinement over the ticket's original assumption list.
**Confidence:** Medium — imp and carapace_husk definitely have head spheres (confirmed by code); the question is whether the design intent is to wire mouth to all slugs with head meshes or only to the "original blob" types. Conservative assumption: wire to all that have head mesh objects, which includes imp and carapace_husk. Spec agent should confirm.
