# Checkpoint Log: M25-MTE Test Breaker Run
**Ticket:** `project_board/25_milestone_25_enemy_editor_visual_expression/in_progress/06_mouth_and_tail_extras.md`
**Stage:** TEST_BREAK
**Run ID:** 2026-04-15T04-00-00Z-test-break

---

### [M25-MTE] TEST_BREAK — Spec gap: location formula sign correctness not tested

**Would have asked:** MTE-4-AC-1 says mouth location = head_center + Vector((+head_radii.x, 0, 0)) and MTE-4-AC-2 says tail = body_center + Vector((-body_radii.x, 0, 0)). No existing test verifies the sign of the x offset. Should we add tests that verify the mouth location X coordinate is GREATER than head_center.x and the tail location X is LESS than body_center.x?

**Assumption made:** Yes. This is a high-risk implementation mistake (accidentally negating the head offset). We add tests that capture the actual location args passed to the patched functions and verify sign correctness relative to the enemy's zone geometry center. Conservative approach: test that mouth_location[0] > 0.0 (assumes head center is near origin) and tail_location[0] < 0.0 (assumes body center is also near origin with negative surface).

**Confidence:** Medium — the exact coordinate values depend on build_options (body/head size), but we can verify directional correctness (mouth is on the +X side, tail on -X side) given default builds.

---

### [M25-MTE] TEST_BREAK — Spec gap: module-level constant constraints not tested

**Would have asked:** MTE-5-AC-5 requires `_MOUTH_FANG_X_RATIO < _MOUTH_BEAK_X_RATIO`. MTE-6-AC-2 implies `_TAIL_WHIP_XY_RATIO < _TAIL_SEG_XY_RATIO`. MTE spec also says `_TAIL_CURLED_Z_RATIO < _TAIL_CURLED_X_RATIO`. No existing test reads these constants from blender_utils and asserts the constraint. Should we test these constraints?

**Assumption made:** Yes. These are spec-mandated invariants that an implementation could violate accidentally. Tests should import the constants from `src.core.blender_utils` and assert the relationships. These tests will be RED until implementation exists (correct Test Breaker state) and provide a regression guard against accidentally swapping constant values.

**Confidence:** High — spec explicitly mandates these relational constraints.

---

### [M25-MTE] TEST_BREAK — Spec gap: MTE-7-AC-7 import verification not tested

**Would have asked:** MTE-7-AC-7 requires that `create_mouth_mesh` and `create_tail_mesh` are importable from `src.core.blender_utils`. No existing test verifies the import path directly (tests only patch the import in enemy modules). Should we add an explicit import test?

**Assumption made:** Yes. A direct `from src.core.blender_utils import create_mouth_mesh, create_tail_mesh` test is important because it guards against the function being defined elsewhere (e.g., in the enemy module directly) which would break the import contract.

**Confidence:** High — explicit import tests are standard in this codebase (see TestCreateMouthMeshHelperExistence).

---

### [M25-MTE] TEST_BREAK — Spec gap: mutation guard for mouth_enabled=True with mouth_shape=None

**Would have asked:** The spec says when mouth_enabled=True, the builder reads mouth_shape from build_options with default "smile". But what if mouth_shape is explicitly set to None? No existing test covers this mutation. Should we test it?

**Assumption made:** Yes. This is a high-risk mutation case. If the implementation uses `str(self.build_options.get("mouth_shape", "smile"))` then None would become "None" (a string), which is not a valid shape and must fall back to "smile". We add a test that verifies this behavior via coercion AND via the builder not crashing.

**Confidence:** High — this is a classic adversarial mutation that can slip through if the coerce path is bypassed in the geometry layer.

---

### [M25-MTE] TEST_BREAK — Spec gap: all 6 slugs baseline part count regression

**Would have asked:** MTE-8-AC-1 says all 6 animated slugs must have identical part count with default options. Existing tests only verify 3 slugs (spider, slug, claw_crawler) in TestNonBreakingDefaultGuarantee. imp, spitter, carapace_husk are in the adversarial stress test but not in a named baseline-regression test. Should we add explicit baseline regression tests for all 6?

**Assumption made:** Yes. We add parametrized tests for all 6 slugs explicitly named in the slug coverage matrix.

**Confidence:** High.

---

### [M25-MTE] TEST_BREAK — Spec gap: player_slime geometry isolation even when enabled

**Would have asked:** MTE-7-AC-8 says player_slime builder must not call create_mouth_mesh or create_tail_mesh even if mouth_enabled=True. The existing test only checks that player_slime is not in ENEMY_CLASSES. No test verifies that building with player_slime options via options_for_enemy does not fire geometry. Should we test this via the controls-only path?

**Assumption made:** Conservative assumption: we test that options_for_enemy("player_slime", {"mouth_enabled": True}) returns the expected values (the control is stored), but no geometry builder is invoked (because player_slime has no builder class). The test encodes this assumption explicitly with a CHECKPOINT comment.

**Confidence:** Medium — we cannot easily instantiate the player_slime builder (it may not exist), so the test focuses on what we can verify: ENEMY_CLASSES exclusion + options round-trip.

---

### [M25-MTE] TEST_BREAK — Spec gap: tail_length boundary 3.001 not explicitly tested

**Would have asked:** The task prompt specifically calls out boundary value 3.001 as a test case. The existing adversarial test uses 3.01. Are these sufficiently different?

**Assumption made:** Add an explicit test for 3.001 to guard against off-by-epsilon implementation errors (e.g., `>` vs `>=` comparison at boundary).

**Confidence:** High.

---

### [M25-MTE] TEST_BREAK — Spec gap: MTE-6-AC-7 tail length scaling not tested via blender_utils directly

**Would have asked:** MTE-6-AC-7 says create_tail_mesh("whip", 2.0, ...) produces longer geometry than create_tail_mesh("whip", 1.0, ...). This requires calling the actual function (not a mock). Without a real bpy, we can test that create_tail_mesh is callable with length argument and doesn't error. Should we test via blender stub?

**Assumption made:** Yes, test via the conftest stub that create_tail_mesh accepts (shape, length, location) positional args and returns a non-None object via `create_tail_mesh("whip", 2.0, (0,0,0))`. We also test the unknown shape fallback directly on blender_utils. These tests will be RED until implementation.

**Confidence:** High.

---

### [M25-MTE] TEST_BREAK — Spec gap: MTE-1-AC-8 tail_length appears in float block BEFORE mesh floats not tested

**Would have asked:** MTE-1-AC-8 requires tail_length appears in the float section before mesh float controls. The existing ordering test only checks that tail_length appears after the non-float block (indirectly). Should we add an explicit test that tail_length index < first mesh-float control index?

**Assumption made:** Yes. This ordering constraint is subtle and could be violated if tail_length accidentally lands in the non-float block or after mesh floats.

**Confidence:** High.

---

## Summary of gaps exposed

| Gap | Severity | Test file target |
|-----|----------|-----------------|
| Location sign correctness (MTE-4-AC-1/2) | HIGH | test_mouth_tail_geometry_adversarial.py |
| Constant constraints (MTE-5-AC-5, MTE-6 invariants) | HIGH | test_mouth_tail_geometry_adversarial.py |
| Import verification MTE-7-AC-7 | HIGH | test_mouth_tail_geometry_adversarial.py |
| Mutation guard: mouth_shape=None with mouth_enabled=True | HIGH | test_mouth_tail_controls_adversarial.py |
| All 6 slugs baseline regression (MTE-8-AC-1) | HIGH | test_mouth_tail_geometry_adversarial.py |
| player_slime geometry isolation | MEDIUM | test_mouth_tail_controls_adversarial.py |
| tail_length 3.001 boundary | MEDIUM | test_mouth_tail_controls_adversarial.py |
| tail_length in float block before mesh floats | MEDIUM | test_mouth_tail_controls_adversarial.py |
| tail length scaling via blender_utils | MEDIUM | test_mouth_tail_geometry_adversarial.py |
| Frontend: tail_enabled toggle regression existing controls | LOW | BuildControls.mouthTail.adversarial.test.tsx |
