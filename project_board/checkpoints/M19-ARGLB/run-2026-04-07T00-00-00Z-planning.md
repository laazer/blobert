# Checkpoint Log: M19-ARGLB — PLANNING

Ticket: `project_board/19_milestone_19_3d_model_quick_editor/in_progress/assets_router_and_glb_viewer.md`

---

### [M19-ARGLB] PLANNING — scaffold already implemented

**Would have asked:** The ticket description says "Implement `routers/assets.py`" and "build `GlbViewer.tsx`", but both files already exist at `asset_generation/web/backend/routers/assets.py` and `asset_generation/web/frontend/src/components/Preview/GlbViewer.tsx` with substantive implementation code that appears to satisfy the acceptance criteria. Is this ticket asking for net-new implementation or verification, testing, and potential gap-filling of the existing scaffold?

**Assumption made:** The scaffold was created as part of the M19 planning commit (`49ba13a`). The ticket is now asking agents to: (1) write tests that verify the acceptance criteria against the existing implementation, (2) identify and fill any gaps between the scaffold and the spec, and (3) confirm the full integration works. No wholesale rewrite is needed — targeted gap analysis then spec/test/impl progression.

**Confidence:** High

---

### [M19-ARGLB] PLANNING — `GlbViewer` clip name exposure design issue

**Would have asked:** In `GlbViewer.tsx`, `setAvailableClips` is assigned to `useAppStore((s) => s.setActiveAnimation)` with a comment "we reuse the store's active anim". This is incorrect: setting the active animation to the first clip name on load conflates "expose available clips" with "set active clip". Should the store gain a `setAvailableClips` action distinct from `setActiveAnimation`, or should `AnimationControls` always receive real clip names from the model via a store slice?

**Assumption made:** The Spec Agent should mandate adding a `availableClips: string[]` slice and `setAvailableClips(names: string[])` action to the Zustand store. `GlbViewer.Model` calls `setAvailableClips(names)` on model load. `AnimationControls` reads `availableClips` from the store instead of relying on a prop with a hardcoded fallback. This fixes the misuse while satisfying the acceptance criterion "one button per clip name from the 13-clip canonical list".

**Confidence:** High

---

### [M19-ARGLB] PLANNING — path traversal test coverage gap

**Would have asked:** The `assets.py` router has path-jail logic, but there are no pytest tests for it. The acceptance criterion `GET /api/assets/../../main.py` returns HTTP 400 is specified but unverified. Should backend pytest tests use `httpx.AsyncClient` with `app` mounted, or a standalone unit test of the path resolution logic?

**Assumption made:** Use `httpx.AsyncClient` with `ASGITransport(app=app)` — the same pattern used in FastAPI's own test recommendations and consistent with how the project's `requirements.txt` includes `httpx`. This tests the full router dispatch + guard logic end-to-end without a running server.

**Confidence:** High
