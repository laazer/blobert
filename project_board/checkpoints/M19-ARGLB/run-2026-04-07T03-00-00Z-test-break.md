# Checkpoint Log — M19-ARGLB Test Breaker Run

**Run ID:** run-2026-04-07T03-00-00Z-test-break
**Agent:** Test Breaker Agent
**Stage:** TEST_BREAK
**Ticket:** project_board/21_milestone_21_3d_model_quick_editor/in_progress/assets_router_and_glb_viewer.md

---

### [M19-ARGLB] TEST_BREAK — null-byte injection behavior
**Would have asked:** When a client sends `animated_exports/file.glb%00.txt`, does Python's `pathlib.Path` raise `ValueError` (caught by `except ValueError` → 400), or does something upstream strip the null byte before the guard sees it? FastAPI may strip or reject null bytes at the ASGI/HTTP parsing layer before routing.
**Assumption made:** Conservative assumption: the null byte in a percent-encoded URL (`%00`) is decoded by FastAPI to a literal null byte `\x00` in the path string. Python's `pathlib.Path` raises `ValueError` for embedded null bytes, which the `except ValueError` block catches and returns HTTP 400. However, if the ASGI server (uvicorn/starlette) rejects null bytes at the HTTP layer before routing (returning 400 from middleware), the end result is still 400 — which satisfies the security requirement. The test asserts 400 regardless of which layer fires. Marked with `# CHECKPOINT`.
**Confidence:** Medium

### [M19-ARGLB] TEST_BREAK — double-encoded traversal %252e%252e behavior
**Would have asked:** `%252e%252e` — FastAPI decodes percent-encoding once: `%25` → `%`, so `%252e` → `%2e` (literal). The path guard receives the string `%2e%2e/main.py`. `Path("%2e%2e")` is a valid path component (not `..`). Does it resolve to within `python_root` (layer 1 passes), then fail layer 2 (not in export dirs) with 403?
**Assumption made:** Yes: the double-encoded path resolves to `python_root / "%2e%2e" / "main.py"`, which is within `python_root` (layer 1 passes), and `"%2e%2e"` is not in `_EXPORT_DIRS` (layer 2 returns 403). The test asserts 403. Marked with `# CHECKPOINT`.
**Confidence:** High

### [M19-ARGLB] TEST_BREAK — list endpoint with export dir having no .glb or .json files
**Would have asked:** When `animated_exports/` exists and contains only non-.glb/non-.json files (e.g., `.txt`, `.bin`, `.py`), should `GET /api/assets` return `{"assets": []}` or is there risk the implementation incorrectly includes non-matching files?
**Assumption made:** The MIME filter `if f.suffix in _MIME` correctly excludes non-.glb/.json files. The list returns `{"assets": []}`. This is a mutation test to verify the filter is not accidentally bypassed. Marked with `# CHECKPOINT`.
**Confidence:** High

### [M19-ARGLB] TEST_BREAK — uppercase extension .GLB not treated as .glb
**Would have asked:** Does the MIME dict lookup use `resolved.suffix` (case-sensitive) so `.GLB` falls through to `application/octet-stream`?
**Assumption made:** Yes: `Path.suffix` is case-sensitive; `.GLB` != `.glb`. The `_MIME.get(resolved.suffix, "application/octet-stream")` returns `application/octet-stream`. Test verifies this is correct per ARGLB-2 edge-case note. This is a mutation test against the assumption that MIME selection is case-insensitive.
**Confidence:** High

### [M19-ARGLB] TEST_BREAK — within-jail traversal that resolves back into export dir
**Would have asked:** A path like `animated_exports/../animated_exports/real_file.glb` — httpx will normalize this to `animated_exports/real_file.glb` before sending. So the server never sees the `..` segment. Does this create a false sense of security that traversal is blocked, when in fact the path was normalized by the client and served correctly?
**Assumption made:** httpx normalization means this resolves to a valid path anyway. The test documents the normalization behavior and asserts 200 with correct MIME. This reveals the vulnerability: there is no defense against a client that normalizes its own traversal URLs before sending; security relies on the server-side guard catching traversals that survive to the handler. The percent-encoded `%2e%2e` tests remain the security-relevant ones. Marked with `# CHECKPOINT` to document this assumption about normalization.
**Confidence:** High

### [M19-ARGLB] TEST_BREAK — stress: list with many files returns all
**Would have asked:** Does the list endpoint have any implicit pagination, truncation, or file-count limit that could silently drop assets?
**Assumption made:** No limit in the scaffold. Writing a stress test with 50 files to expose any such hidden truncation.
**Confidence:** High

### [M19-ARGLB] TEST_BREAK — directory-as-path returns 404 not 500 (strictened)
**Would have asked:** The existing test asserts `!= 200` to accommodate the current 500 behavior. Should Test Breaker add a separate test that asserts 404 specifically (the preferred behavior), marking it as a red test until the implementation fixes `is_file()` check?
**Assumption made:** Yes: add a separate adversarial test that asserts exactly 404, clearly flagged as "expected to fail until implementation adds is_file() guard." This documents the implementation gap more precisely than the existing soft assertion.
**Confidence:** High
