# Add the repo root and ci/scripts to PATH so scripts like run_tests.sh are accessible from anywhere in the repo
export PATH="$PWD:$PWD/ci/scripts:$PATH"

# Add Godot engine binary to PATH
export PATH="/Applications/Godot.app/Contents/MacOS:$PATH"